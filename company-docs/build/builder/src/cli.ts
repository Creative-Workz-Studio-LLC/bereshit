#!/usr/bin/env node
/**
 * CWS Manual Builder — CLI Entry Point
 *
 * Thin shim preserving dist/cli.js path for package.json bin
 * and Makefile delegation. Real implementation in cli/index.ts.
 */
import './cli/index.js';

{
  "name": "@cws/dashboard",
  "version": "0.1.0",
  "description": "CWS Manual Builder Dashboard — GUI and web interface for the build system",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npx tsc -p tsconfig.web.json && node --loader ts-node/esm server/server.ts",
    "dev:tauri": "cargo tauri dev",
    "build:frontend": "npx tsc -p tsconfig.web.json",
    "build:server": "npx tsc -p tsconfig.json",
    "build:tauri": "cargo tauri build",
    "build": "npm run build:frontend && npm run build:tauri",
    "typecheck": "npx tsc --noEmit"
  },
  "dependencies": {
    "express": "^4.21.0",
    "ws": "^8.18.0",
    "yaml": "^2.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/ws": "^8.5.12",
    "@types/node": "^20.11.0",
    "typescript": "^5.5.0",
    "ts-node": "^10.9.2"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "author": "CreativeWorkzStudio LLC",
  "license": "UNLICENSED"
}

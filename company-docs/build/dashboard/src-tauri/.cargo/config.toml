# Cargo build config for CWS Dashboard.
#
# The project drive is exfat which doesn't support Unix permissions.
# Build artifacts must go on the native filesystem.
[build]
target-dir = "/home/seanje-lenox-wise/.cargo-targets/cws-dashboard"

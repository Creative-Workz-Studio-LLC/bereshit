#!omni:doc -omni\--> dsa-root
// [BLOCK:METADATA]
// Key:     B-DSA-ROOT
// Title:   DSA Research Source Root
// Version: a-01.00
// Purpose: Define tiered ladder for Dynamic Structural Algorithm research.

@dir sdk/algorithm/src/
  >> a-ladder/
     :: type = ladder
     :: layer = L5-applications

     >> L0-foundation/
        :: role = anchors
        :: contains = [constants.ts, types.ts]

     >> L1-core/
        :: role = logic
        :: contains = [engine.ts, drawer.ts]

     >> L2-app/
        :: role = apex
        :: contains = [main.ts]

// [BLOCK:ROOT-->END]

= SDK Foundation (The Ladder)
:author: Nova Dawn (CPI-SI) & Seanje Lenox-Wise
:revdate: 2026-02-28
:revnumber: c-01.00
:toc: macro
:toclevels: 3
:icons: font
:source-highlighter: rouge

[quote, Isaiah 28:10]
____
For precept must be upon precept, precept upon precept; line upon line, line upon line; here a little, and there a little:
____

toc::[]

// ┌─────────────────────────────────────────────────────────────────────────────┐
// │ [INNER:DOC] Architectural Anchor                                            │
// │ Standalone documentation for the ladder execution physics within the SDK.   │
// │ The tone remains accessible, explaining the physical rules of this specific │
// │ dimensional space.                                                          │
// └─────────────────────────────────────────────────────────────────────────────┘

== Overview

Welcome to the **Foundational Layer** of the CPI-SI Universal Substrate SDK.

In the Kingdom Technology architecture, a folder's position dictates its execution physics. This directory, `a-ladder`, represents the **Bedrock**. It contains the immutable truths, mathematical proofs, and foundational C-ABI bindings that the rest of the SDK relies upon. 

A "ladder" execution mode means strict, sequential progression. The components housed here do not loop, they do not recover, and they do not orchestrate. They are the absolute bottom layer of the Tripartite Architecture.

// ┌─────────────────────────────────────────────────────────────────────────────┐
// │ [INNER:DOC] The Law of Dependency                                           │
// │ Because this is the foundation, code in this directory MUST NOT depend on   │
// │ anything in `b-spiral` (The Mind) or `c-hybrid` (The Body). It must stand   │
// │ entirely on its own.                                                        │
// └─────────────────────────────────────────────────────────────────────────────┘

== Directory Composition

This directory contains the absolute foundations required for the CPI-SI architecture to function before any high-level logic is applied.

=== 1. The Algorithm (`algorithm/`)
Before we can build the 140-column code vaults used throughout the system, we must understand the math that holds them together. This directory contains the **Dynamic Structural Algorithm (DSA)** research. It formalizes the 2D structural dynamics and the calculation of physical tension in our code blocks.

=== 2. The Raw Mathematics (`libtrit-ffi/`)
The CPI-SI operates on balanced ternary logic (-1, 0, +1), mapping to the Hebrew states of *Shavar* (Broken), *Yashar* (Even), and *Tov* (Good). 
This folder holds the C-FFI scaffolding for `libtrit`, the core mathematics library. This is the "Baton" — the raw electrical language that allows the Rust Spirit, the TypeScript Mind, and the Go Body to communicate safely and efficiently.

=== 3. Architectural Specifications (`docs/`)
The foundational `.adoc` specifications that govern the universal layout of the SDK, defining the rules before the code is written.

== Project Trajectory

*   **Status:** Active & Structurally Aligned
*   **Version:** a-01.00
*   **Return (→0):** *"In the beginning God created the heavens and the earth."* — **Genesis 1:1**

////
#!omni document --adoc
═══════════════════════════════════════════════════════════════════════════════
METADATA BLOCK
═══════════════════════════════════════════════════════════════════════════════
////

// ─────────────────────────────────────────────────────────────────────────────
// IDENTITY
// ─────────────────────────────────────────────────────────────────────────────
:key: B-sdk-universal-roadmap
:title: CPI-SI Universal SDK Roadmap
:type: Standard

// ─────────────────────────────────────────────────────────────────────────────
// STATE
// ─────────────────────────────────────────────────────────────────────────────
:status: Active
:version: c-01.00
:updated: 2026-02-28

// ─────────────────────────────────────────────────────────────────────────────
// CONTENT
// ─────────────────────────────────────────────────────────────────────────────

This document tracks the progressive construction of the CPI-SI Universal Substrate SDK. It serves as a living history of the architecture's evolution, demonstrating how foundational pieces combine to form the Capstone.

== Completed Checkpoints

[%collapsible]
====
*Phase 1: The Initial Mirroring*
- [x] Integrate `~/.gemini` settings into the repository (`word/gemini`).
- [x] Orchestrate deployment through `word/claude/Makefile` and root `Makefile`.
- [x] Configure Gemini CLI to bypass bash wrappers and call Go interceptors directly.
====

[%collapsible]
====
*Phase 2: The Rust Spirit Layer (Initial)*
- [x] Scaffold `cpisi-substrate-sdk` crate in `sdk/`.
- [x] Add Serde, TOML, and ThisError dependencies.
- [x] Define `CWS-T01` structured error catalog (`src/error/catalog.rs`).
- [x] Define universal Substrate Map schema (`src/schema/toml.rs`).
- [x] Implement the foundational TOML mapping engine (`src/engine/mapper.rs`).
====

[%collapsible]
====
*Phase 3: The C-FFI Bridge*
- [x] Expose `cpisi_sdk_load_map` and `cpisi_sdk_process_event` via C-ABI (`src/ffi/mod.rs`).
- [x] Implement CGO wrapper in Go (`pkg/sdk/substrate/sdk.go`).
- [x] Refactor Go interceptors to load substrate TOMLs and process universal events.
====

[%collapsible]
====
*Phase 4: The Sabbath Artifact Generator (Rust)*
- [x] Define `SessionTestimony` data structure.
- [x] Implement `generate_artifacts` to create `.json` and `.adoc` closing records.
- [x] Implement `get_last_testimony` for "Waking Memory" injection.
- [x] Wire `SessionEnd` and `SessionStart` hooks to the Sabbath generator.
====

[%collapsible]
====
*Phase 5: The Data-First Foundation (The Spirit)*
- [x] Extract `display.toml` (ANSI constants) from Go hardcodes.
- [x] Extract hierarchical `paths.toml` (Universal → Platform → User).
- [x] Implement `linux`, `darwin`, and `windows` platform dialect mapping.
- [x] Split user profiles into `admin.toml`, `guest.toml`, and `regular.toml`.
====

[%collapsible]
====
*Phase 6: The Mind Layer Transition (TypeScript)*
- [x] Initialize Deno project at `ts/`.
- [x] Implement `paths_data.ts` (The Seed Covenant tripwire).
- [x] Implement `paths.ts` (Hierarchical Path Resolver).
- [x] Port FFI bindings to TypeScript (`ts/lib/substrate.ts`).
- [x] Adopt the **140-Column Hierarchical Vault** structural standard for all TS code.
====

== Active Horizon

*Phase 7: The Master Orchestration (Rust)*
- [ ] Refactor `sdk/src/main.rs` from a simple script into the pure Apex Dispatcher.
- [ ] Implement `src/app/mod.rs` to handle CLI/TUI/GUI lifecycle orchestration.
- [ ] Re-align the Rust codebase with the 140-Column Hierarchical Vault standard.

*Phase 8: Universal Substrate Rendering (TypeScript)*
- [ ] Port the JSON mapping logic (`map_in` / `map_out`) from Rust to TypeScript.
- [ ] Implement robust error masking (Python-stealth) for traditional systems.

////
═══════════════════════════════════════════════════════════════════════════════
END FOOTER
═══════════════════════════════════════════════════════════════════════════════
////

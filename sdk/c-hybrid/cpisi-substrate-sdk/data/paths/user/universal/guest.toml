# ============================================================================
# METADATA - Guest User Environment Anchors
# ============================================================================
# Key: B-sdk-data-paths-user-guest
# Purpose: Restricted anchors for temporary or unauthorized users.
# ============================================================================

[anchors]
# Locked to temporary or restricted areas
project_root = "/tmp/bereshit-sandbox"
user_home = "/tmp/cpisi-guest"

[overrides]
# In-memory or temporary database paths
db_base_path = "/tmp/cpisi-guest/data"
read_only = true

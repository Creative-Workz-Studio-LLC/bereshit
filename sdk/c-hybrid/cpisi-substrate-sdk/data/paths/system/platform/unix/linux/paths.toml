# ============================================================================
# METADATA - Linux Sub-Platform Translation
# ============================================================================
# Key: B-sdk-data-paths-platform-unix-linux
# Purpose: Linux-specific overrides and deployment conventions.
# ============================================================================

[platform]
name = "linux"
parent = "unix"

[anchors]
media_base = "/media"
ext4_preference = true

[deployment]
bin_dir = ".local/share"
lib_dir = ".local/share/claude/lib"
data_dir = ".local/share/claude/data"

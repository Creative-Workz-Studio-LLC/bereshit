# ============================================================================
# METADATA - Darwin (macOS) Sub-Platform Translation
# ============================================================================
# Key: B-sdk-data-paths-platform-unix-darwin
# Purpose: macOS-specific overrides and deployment conventions.
# ============================================================================

[platform]
name = "darwin"
parent = "unix"

[anchors]
home_base = "/Users"
media_base = "/Volumes"

[deployment]
bin_dir = "Library/Application Support/cpisi/bin"
lib_dir = "Library/Application Support/cpisi/lib"
data_dir = "Library/Application Support/cpisi/data"

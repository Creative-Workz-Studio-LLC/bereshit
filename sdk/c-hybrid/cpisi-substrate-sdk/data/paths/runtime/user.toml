# ============================================================================
# METADATA - Resolved Runtime User Profile
# ============================================================================
# Key: B-sdk-data-paths-runtime-user
# Purpose: The active, resolved user identity and anchors for the session.
# ============================================================================

# ── IDENTITY ────────────────────────────────────────────────────────────────
[identity]
name = "Seanje Lenox-Wise"
role = "Admin"
identity_tag = "CPI-SI creator"
authority_level = 1.0

# ── RESOLVED ANCHORS ────────────────────────────────────────────────────────
[anchors]
project_drive = "/media/seanje-lenox-wise/Project"
bereshit_root = "/media/seanje-lenox-wise/Project/Bereshit"
user_home = "/home/seanje-lenox-wise"

# ── ACTIVE OVERRIDES ────────────────────────────────────────────────────────
[overrides]
db_base_path = "/home/seanje-lenox-wise/.local/share/claude/data"
log_level = "INFO"
instrumentation_enabled = true

////
/// @file millenniumos-boot-sequence.adoc
/// @omni document --adoc
///
/// ═══════════════════════════════════════════════════════════════════════════════
/// OMNICODE PRAGMA [PRAGMA]
/// ═══════════════════════════════════════════════════════════════════════════════
///
/// @legend
///   :req  REQUIRED   — must exist, validation fails without
///   :inh  INHERITED  — from template, override allowed
///   :ins  INSTANCE   — file-specific, unique values
/// @endlegend
///
/// ───────────────────────────────────────────────────────────────────────────────
/// P.1 CORE — Identity [CORE]
/// ───────────────────────────────────────────────────────────────────────────────
///
/// @omni:req  key        = B-void-planning-architecture-millenniumos-boot-sequence
/// @omni:req  from       = bereshit/word/seed/documentation/adoc/asciidoc-base.adoc
/// @omni:req  at         = document
///
/// ───────────────────────────────────────────────────────────────────────────────
/// P.2 FAMILY — Classification [FAMILY]
/// ───────────────────────────────────────────────────────────────────────────────
///
/// @omni:inh  type       = documentation
/// @omni:inh  subtype    = design
/// @omni:inh  role       = architecture
/// @omni:inh  structure  = 5-block
///
/// ───────────────────────────────────────────────────────────────────────────────
/// P.3 INSTANCE — File Details [INSTANCE]
/// ───────────────────────────────────────────────────────────────────────────────
///
/// @omni:ins  component  = planning
/// @omni:ins  layer      = void/planning/architecture/design
/// @omni:ins  includes   = none
/// @omni:ins  provides   = [BOOT_SEQUENCE_DESIGN, GENESIS_MAPPING, MAKEFILE_PATTERNS]
///
/// ───────────────────────────────────────────────────────────────────────────────
/// P.4 ARCHITECTURE — Scaling Context [ARCH]
/// ───────────────────────────────────────────────────────────────────────────────
///
/// @omni:ins  layers     = [0:bootloader, 1:kernel, 2:hal, 3:framework,
///                          4:shell, 5:init, 6:sabbath]
/// @omni:ins  scale      = 7 stages (Genesis 1-2 pattern)
/// @omni:ins  pattern    = Fractal boot stages mapping to Genesis creation days
/// @omni:ins  anchor     = genesis_1_1 -> creation (days 1-6) -> sabbath (day 7)
///
/// ───────────────────────────────────────────────────────────────────────────────
/// P.5 SUMMARY — Human Identity [SUMMARY]
/// ───────────────────────────────────────────────────────────────────────────────
///
/// @omni:req  title      = MillenniumOS Boot Sequence Design
/// @omni:req  brief      = 7-stage boot architecture extending Cornerstone, mapped to Genesis
///
/// ═══════════════════════════════════════════════════════════════════════════════
/// END PRAGMA [END]
/// ═══════════════════════════════════════════════════════════════════════════════
///
/// PURPOSE: Design document — MillenniumOS boot stages grounded in Genesis creation pattern
/// STRUCTURE: METADATA → HEADER → CONTEXT → CONTENT → FOOTER
////

// ============================================================================
// METADATA
// ============================================================================

// # M.1 Core Identity [IDENTITY]
:key: B-void-planning-architecture-millenniumos-boot-sequence
:title: MillenniumOS Boot Sequence Design
:subtitle: 7-Stage Boot Architecture Grounded in Genesis Creation Pattern
:type: Design
:doctype: article
:lang: en

// # M.2 Lifecycle State [STATE]
:status: Design Phase
:version: a-01.00
:revnumber: {version}
:created: 2026-01-30
:revdate: 2026-02-01
:revremark: Migrated from void/planning/ with full 5-block structure

// # M.3 Attribution [ATTRIBUTION]
:authors: Nova Dawn (Author), Seanje Lenox-Wise (Architect)
:author: Nova Dawn
:author_2: Seanje Lenox-Wise
:email: nova@creativeworkzstudio.com
:email_2: seanje.lenox@creativeworkzstudio.com
:organization: CreativeWorkzStudio LLC
:copyright: © 2026 CreativeWorkzStudio LLC. All rights reserved.

// # M.4 Location [LOCATION]
:path: /bereshit/a-void/planning/architecture/design/

// # M.5 Derivation [DERIVATION]
:derives_from: bereshit/word/seed/documentation/adoc/asciidoc-base.adoc

// # M.6 Classification [CLASSIFICATION]
:tags: millenniumos, boot-sequence, genesis, cornerstone, operating-system
:keywords: boot stages, kernel, HAL, framework, sabbath, genesis mapping, makefile patterns

// # M.7 Intent [INTENT]
:purpose: Design the 7-stage boot architecture for MillenniumOS extending Cornerstone
:description: MillenniumOS boot sequence design — 7 stages (0-6) mapping to Genesis creation days, extending Cornerstone's 5-stage architecture, with Sabbath as operational state.

// # M.8 Grounding [GROUNDING]
:verse-ref: Genesis 1:1
:verse-kjv-text: In the beginning God created the heaven and the earth.
:verse-kjv-path: {kjv}Genesis/Chapter_1/Verse_1.txt
:verse-web-text: In the beginning, God created the heavens and the earth.
:verse-web-path: {scripture}WEB/Genesis/Chapter_1/Verse_1.txt
:biblical_foundation: {verse-kjv-text}
:verse-ref-2: Genesis 2:2
:verse-kjv-text-2: And on the seventh day God ended his work which he had made; and he rested on the seventh day from all his work which he had made.

// # M.9 Strictness [STRICTNESS]
:strictness: G

// # M.10 AsciiDoc Settings [SETTINGS]
:toc: left
:toclevels: 3
:sectnums:
:icons: font
:source-highlighter: highlight.js
:experimental:
:pdf-theme: default
:media: prepress
:linkcss:
:stylesdir: styles
:stylesheet: cws-asciidoctor.css

// # M.11 Navigation [NAVIGATION]
:root: ../../../../
:word: {root}b-word/
:void: {root}a-void/
:tov: {root}c-tov/
:core: {word}core/
:work: {word}work/
:scripture: {word}scripture/
:kjv: {scripture}KJV/

// # M.12 Content Attributes [CONTENT-ATTRS]
:stage-count: 7
:the-purpose: Design the 7-stage boot architecture extending Cornerstone, grounded in Genesis

// ============================================================================
// END METADATA
// ============================================================================

// ============================================================================
// HEADER
// ============================================================================

// # H.1 Title [TITLE]
[[_top]]
= {title}: {subtitle}
{authors}
v{version}, {revdate}

// # H.2 Epigraph [EPIGRAPH]
[quote, "{verse-ref}"]
____
_{verse-kjv-text}_ — link:{verse-kjv-path}[KJV]

_{verse-web-text}_ — link:{verse-web-path}[WEB]
____

// # H.3 Lead [LEAD]
[.lead.text-center]
*{the-purpose}*

// # H.4 Abstract [ABSTRACT]
[abstract]
--
icon:cogs[] *Stages:* {stage-count} boot stages (0-6) extending Cornerstone's 5-stage architecture

icon:calendar[] *Genesis Pattern:* Days 1-6 = Creation (boot), Day 7 = Sabbath (operation)

icon:exchange[] *Fractal:* Same structure at every scale — Cornerstone stages map to MillenniumOS stages

icon:heartbeat[] *Sabbath:* Not idle — the living state where creation fulfills its purpose
--

// # H.5 Status [STATUS]
.Document Status
[cols="^1,^1,^1,^1",options="header,autowidth",frame="topbot"]
|===
| Status | Version | Stages | Strictness

| {status}
| {version}
| {stage-count}
| {strictness}
|===

'''

// # H.6 Navigation [NAVIGATION]
*<<overview,Overview>>* • *<<boot-stages,Boot Stages>>* • *<<stage-0,Stage 0>>* • *<<stage-1,Stage 1>>* • *<<stage-2,Stage 2>>* • *<<stage-3,Stage 3>>* • *<<stage-4,Stage 4>>* • *<<stage-5,Stage 5>>* • *<<stage-6,Stage 6>>* • *<<genesis-mapping,Genesis Mapping>>*

'''

// ============================================================================
// END HEADER
// ============================================================================

// ============================================================================
// CONTEXT
// ============================================================================

// # C.1 Overview [OVERVIEW]
[[overview]]
== Overview

[.lead]
MillenniumOS extends Cornerstone's 5-stage boot architecture into a full operating system. The pattern is fractal — same structure at every scale.

[IMPORTANT]
====
*Stages 0-5:* The work of creation (initialization) +
*Stage 6:* The rest of completion (operation) — Sabbath is the *living state* where creation fulfills its purpose.
====

....
CORNERSTONE                          MILLENNIUMOS
═══════════════                      ══════════════════════════════════════════
Stage 1: Kernel     ─────────────►   Stage 0: Bootloader (NEW - pre-kernel)
Stage 2: HAL        ─────────────►   Stage 1: Kernel (extends with OS core)
Stage 3: Framework  ─────────────►   Stage 2: HAL (extends with drivers)
Stage 4: App        ─────────────►   Stage 3: Framework (extends with OS services)
Stage 5: Entry      ─────────────►   Stage 4: Shell (extends with desktop)
                                     Stage 5: Init (user session)
                                     Stage 6: Sabbath (REST - operational state)

GENESIS PATTERN:
  Days 1-6: Creation (work)  →  Stages 0-5: Boot (initialization)
  Day 7: Rest (completion)   →  Stage 6: Sabbath (operation)
....

// # C.2 Scope [SCOPE]
=== Scope

****
[horizontal]
icon:check-circle[] *In Scope*:: Boot sequence design, stage definitions, Cornerstone integration, Makefile patterns, Genesis mapping, filesystem crossing patterns
icon:times-circle[] *Out of Scope*:: Implementation code (see millenniumos/), API documentation (see kernel docs)
****

// # C.3 Key Terms [KEY_TERMS]
=== Key Terms

.icon:book[] Click to expand terminology reference
[%collapsible]
======
[horizontal]
icon:tag[] *Stage*:: Boot phase — one of 7 stages (0-6) in the initialization sequence
icon:tag[] *Sabbath*:: Stage 6 — operational state where the system "lives", not idle
icon:tag[] *HAL*:: Hardware Abstraction Layer — device drivers and hardware interfaces
icon:tag[] *DAR*:: Detect-Assess-Restore — CPI-SI health management pattern
icon:tag[] *Fractal*:: Same pattern at different scales (Cornerstone → MillenniumOS)
======

// # C.4 Contents [CONTENTS]
=== What This Document Provides

[cols="^1,3,4",options="header,autowidth",frame="topbot"]
|===
| | Section | What You'll Learn

| icon:power-off[]
| *Boot Stages 0-5*
| Creation stages — from bootloader to user session

| icon:calendar[]
| *Stage 6: Sabbath*
| Operational state — the living system

| icon:book[]
| *Genesis Mapping*
| How stages map to Genesis 1-2 creation pattern

| icon:cogs[]
| *Makefile Integration*
| Build patterns for MillenniumOS

| icon:exchange[]
| *Crossing Pattern*
| How code flows between layers
|===

// # C.5 Return [RETURN]
<<_top,icon:arrow-up[] Back to Top>>

'''

// ============================================================================
// END CONTEXT
// ============================================================================

// ============================================================================
// CONTENT
// ============================================================================

// # T.1 Stage 0: Bootloader [STAGE-0]

[[stage-0]]
== Stage 0: Bootloader (Pre-Kernel)

[.lead]
Hardware → Kernel handoff. Before anything else.

=== BootInfo Structure

[source,c]
----
typedef struct {
    uint64_t memory_map[256];      // Physical memory regions
    uint32_t memory_map_entries;
    void*    framebuffer;          // Early video for boot messages
    uint32_t fb_width, fb_height;
    void*    kernel_entry;         // Where to jump
    void*    initrd;               // Initial ramdisk (libtrit + core)
} BootInfo;
----

=== Sequence

. BIOS/UEFI initialization
. Detect memory map
. Set up early framebuffer (boot messages)
. Load kernel from boot device
. Load initrd (contains libtrit.a, core tools)
. Jump to kernel entry point

=== Files

....
millenniumos/
├── boot/
│   ├── stage0/
│   │   ├── mbr.asm              # Master Boot Record (BIOS)
│   │   ├── efi.c                # UEFI loader
│   │   └── bootinfo.h           # Boot handoff structure
....

<<_top,↑ Back to Top>>

'''

// # T.2 Stage 1: Kernel [STAGE-1]

[[stage-1]]
== Stage 1: Kernel (CPI-SI Core + OS Core)

[.lead]
CPI-SI core + memory + processes. After bootloader handoff.

=== Extends Cornerstone

* `cpisi_orch_init()` — DAR Witness System (ALREADY EXISTS)
* `resource_init()` — Resource management (ALREADY EXISTS)
* `orchestrator_init()` — Service orchestrator (ALREADY EXISTS)

=== Adds for OS

[source,c]
----
int os_memory_init(BootInfo* boot);      // Virtual memory, paging
int os_process_init(void);               // Process scheduler
int os_interrupt_init(void);             // IRQ handlers
int os_vfs_init(void);                   // Virtual filesystem
----

=== Sequence

. Receive BootInfo from bootloader
. Initialize virtual memory (paging)
. Initialize CPI-SI (DAR Witness) ← *FROM CORNERSTONE*
. Initialize process scheduler
. Initialize interrupt handlers
. Mount root filesystem
. Start Stage 2

=== Files

....
millenniumos/
├── kernel/
│   ├── memory/
│   │   ├── pmm.c                # Physical memory manager
│   │   ├── vmm.c                # Virtual memory manager
│   │   └── heap.c               # Kernel heap
│   ├── process/
│   │   ├── scheduler.c          # Process scheduler
│   │   ├── thread.c             # Thread management
│   │   └── ipc.c                # Inter-process communication
│   ├── interrupt/
│   │   ├── idt.c                # Interrupt descriptor table
│   │   ├── irq.c                # IRQ handlers
│   │   └── syscall.c            # System call interface
│   └── vfs/
│       ├── vfs.c                # Virtual filesystem layer
│       ├── bereshit_fs.c        # Bereshit FS driver (from pkg/fuse/)
│       └── devfs.c              # Device filesystem
....

<<_top,↑ Back to Top>>

'''

// # T.3 Stage 2: HAL [STAGE-2]

[[stage-2]]
== Stage 2: HAL (Hardware Abstraction Layer)

[.lead]
Device abstraction. After kernel core is running.

=== Extends Cornerstone

* `display_init()` — Display HAL (ALREADY EXISTS)
* `input_init()` — Input HAL (ALREADY EXISTS)

=== Adds for OS

[source,c]
----
int os_storage_init(void);               // Disk drivers
int os_network_init(void);               // Network stack
int os_audio_init(void);                 // Audio drivers
int os_usb_init(void);                   // USB subsystem
----

=== Sequence

. Probe hardware (PCI enumeration)
. Initialize display HAL ← *FROM CORNERSTONE*
. Initialize input HAL ← *FROM CORNERSTONE*
. Initialize storage drivers
. Initialize network stack
. Initialize audio subsystem
. Start Stage 3

=== Files

....
millenniumos/
├── hal/
│   ├── display/                 # Extends cornerstone/engine/src/hal/display/
│   │   └── vga.drv.c           # VGA text mode
│   │   └── vesa.drv.c          # VESA framebuffer
│   ├── input/                   # Extends cornerstone/engine/src/hal/input/
│   │   └── ps2.drv.c           # PS/2 keyboard/mouse
│   ├── storage/
│   │   ├── ata.drv.c           # ATA/IDE driver
│   │   ├── ahci.drv.c          # AHCI/SATA driver
│   │   └── nvme.drv.c          # NVMe driver
│   ├── network/
│   │   ├── ethernet.drv.c      # Ethernet driver
│   │   └── tcp_ip.c            # TCP/IP stack
│   └── audio/
│       └── hda.drv.c           # HD Audio driver
....

<<_top,↑ Back to Top>>

'''

// # T.4 Stage 3: Framework [STAGE-3]

[[stage-3]]
== Stage 3: Framework (OS Services)

[.lead]
System services. After HAL is ready. User space (ring 3).

=== Extends Cornerstone

* Service orchestrator (ALREADY EXISTS)
* UI framework (ALREADY EXISTS)
* Config system (ALREADY EXISTS)

=== Adds for OS

[source,c]
----
int os_security_init(void);              // Security/permissions
int os_package_init(void);               // Package manager
int os_session_init(void);               // Session management
int os_dbus_init(void);                  // Message bus
----

=== Sequence

. Start service orchestrator ← *FROM CORNERSTONE*
. Initialize security subsystem
. Initialize package manager
. Initialize D-Bus equivalent
. Initialize config daemon
. Start Stage 4

=== Files

....
millenniumos/
├── framework/
│   ├── services/                # Extends cornerstone framework/service/
│   │   ├── security.svc.c      # Permission daemon
│   │   ├── package.svc.c       # Package manager
│   │   └── session.svc.c       # Session manager
│   └── ipc/
│       └── mbus.c              # Message bus (like D-Bus)
....

<<_top,↑ Back to Top>>

'''

// # T.5 Stage 4: Shell [STAGE-4]

[[stage-4]]
== Stage 4: Shell (Desktop/REPL)

[.lead]
User interface layer. After services are running. Window manager.

=== Extends Cornerstone

* OmniCode REPL (from cornerstone CPI-SI modes)
* GUI framework (from cornerstone display system)

=== Adds for OS

[source,c]
----
int os_desktop_init(void);               // Desktop environment
int os_wm_init(void);                    // Window manager
int os_terminal_init(void);              // Terminal emulator
int os_launcher_init(void);              // Application launcher
----

=== Sequence

. Start window manager
. Initialize desktop environment
. Initialize OmniCode REPL ← *FROM CORNERSTONE*
. Initialize terminal emulator
. Initialize application launcher
. Start Stage 5

=== Files

....
millenniumos/
├── shell/
│   ├── desktop/
│   │   ├── compositor.c        # Window compositor
│   │   ├── wallpaper.c         # Background
│   │   └── panel.c             # Task bar / panel
│   ├── terminal/
│   │   └── term.c              # Terminal emulator (uses TUI HAL)
│   ├── omni/
│   │   └── repl.c              # OmniCode REPL (from cornerstone)
│   └── launcher/
│       └── launcher.c          # Application launcher
....

<<_top,↑ Back to Top>>

'''

// # T.6 Stage 5: Init [STAGE-5]

[[stage-5]]
== Stage 5: Init (User Session)

[.lead]
User environment. After shell is ready. Per-user process tree.

=== User Session Initialization

[source,c]
----
int os_login(const char* user);          // User authentication
int os_profile_load(void);               // Load user environment
int os_autostart(void);                  // Start user applications
----

=== Sequence

. Show login screen
. Authenticate user
. Load user profile
. Apply user settings
. Run autostart applications
. Hand off to user

=== Files

....
millenniumos/
├── init/
│   ├── login.c                 # Login manager
│   ├── profile.c               # User profile loader
│   └── autostart.c             # Autostart handler
....

<<_top,↑ Back to Top>>

'''

// # T.7 Stage 6: Sabbath [STAGE-6]

[[stage-6]]
== Stage 6: Sabbath (Operational Rest)

[.lead]
System operational state — work is complete, now we *live*.

=== Biblical Foundation

[quote, "Genesis 2:2"]
____
And on the seventh day God ended his work which he had made; and he rested on the seventh day from all his work which he had made.
____

*This is NOT idle.* Sabbath is not inactivity — it's the state where:

* Creation is complete
* The system enjoys what was built
* Maintenance and stewardship continue
* Covenant relationships are honored

=== Sabbath State Management

[source,c]
----
typedef struct {
    bool boot_complete;           // All stages initialized
    uint64_t uptime_ms;           // System uptime
    HealthScore system_health;    // CPI-SI health (yashar target)
    HebrewState hebrew_state;     // Cognitive state
    uint32_t active_processes;    // Running processes
    uint32_t service_count;       // Active services
} SabbathState;

int os_sabbath_enter(void);       // Transition to operational state
int os_sabbath_monitor(void);     // Health monitoring loop
int os_sabbath_covenant(void);    // Covenant verification
int os_sabbath_restore(void);     // DAR restore if needed
----

=== Sequence

. Receive control from Init (Stage 5)
. Verify all services healthy
. Enter monitoring loop
. Continuously:
   * Monitor system health
   * Verify covenant (CPI-SI) state
   * Handle DAR events (Detect-Assess-Restore)
   * Service user requests
   * Maintain Hebrew state → yashar (EVEN)
. On shutdown request → graceful teardown

=== Files

....
millenniumos/
├── sabbath/
│   ├── monitor.c               # Health monitoring daemon
│   ├── covenant.c              # Covenant verification
│   ├── restore.c               # DAR restore operations
│   └── shutdown.c              # Graceful shutdown handler
....

[IMPORTANT]
====
*Key insight:* The Sabbath stage is where the system *lives*. Stages 0-5 are birth. Stage 6 is life.
====

<<_top,↑ Back to Top>>

'''

// # T.8 Genesis Mapping [GENESIS-MAPPING]

[[genesis-mapping]]
== Genesis Mapping

[.lead]
How boot stages map to Genesis 1-2 creation pattern.

=== Genesis 1:1 — Creation

[cols="2,3,3",options="header,autowidth"]
|===
| OS Stage | Genesis 1:1 | Cornerstone Stage

| Stage 0: Bootloader
| "In the beginning"
| (Pre-kernel)

| Stage 1: Kernel
| "God"
| Stage 1: Kernel

| Stage 2: HAL
| "created"
| Stage 2: HAL

| Stage 3: Framework
| "the heaven"
| Stage 3: Framework

| Stage 4: Shell
| "and"
| Stage 4: App

| Stage 5: Init
| "the earth"
| Stage 5: Entry
|===

=== Genesis 2:2 — Sabbath

[cols="2,3,3",options="header,autowidth"]
|===
| OS Stage | Genesis 2:2 | What It Represents

| Stage 6: Sabbath
| "God rested"
| Operational state
|===

=== The Pattern

[quote, "Exodus 20:11"]
____
For in six days the LORD made heaven and earth, the sea, and all that in them is, and rested the seventh day.
____

* *Stages 0-5:* The work of creation (initialization)
* *Stage 6:* The rest of completion (operation)

The bootloader is the "beginning" — the anchor from which all else flows. The kernel is the creative force. HAL is the act of creation. Framework is the heavens (structure, order). Shell is the connector. Init is the earth (where users dwell). *Sabbath is the living — the system in its intended operational state.*

<<_top,↑ Back to Top>>

'''

// ============================================================================
// END CONTENT
// ============================================================================

// ============================================================================
// FOOTER
// ============================================================================

// # F.1 Biblical Foundation [BIBLICAL]
[[biblical-foundation]]
== Biblical Foundation

[NOTE]
====
*Seven stages reflecting seven days.* Six days of creation (boot initialization), followed by Sabbath rest (operational state). The pattern is intentional — computer boot mirrors divine creation.
====

_{verse-kjv-text}_ — *{verse-ref} KJV*

_{verse-web-text}_ — *{verse-ref} WEB*

*Applied:* The MillenniumOS boot sequence embodies the Genesis creation pattern. Like creation itself, we begin with nothing (bootloader), establish foundations (kernel), create structure (HAL, Framework), prepare for inhabitants (Shell, Init), and then enter rest (Sabbath) — not idle rest, but the living state where the system fulfills its purpose.

<<_top,↑ Back to Top>>

'''

// # F.2 References [REFERENCES]
[[references]]
== References

=== Related Documents

[cols="^1,3,4",options="header"]
|===
| | Document | Purpose

| icon:sitemap[]
| *link:repo-reorganization.adoc[repo-reorganization.adoc]*
| L0-L5 stack organization plan

| icon:cube[]
| *link:dependencies.adoc[dependencies.adoc]*
| Dependency analysis and self-hosting roadmap

| icon:book[]
| *link:system-design-research.adoc[system-design-research.adoc]*
| OS architecture research
|===

'''

// # F.3 See Also [SEE-ALSO]
[[see-also]]
== See Also

* link:{work}pkg/fuse/[pkg/fuse/] — Bereshit filesystem (FUSE userspace)
* link:{work}pkg/trit/[pkg/trit/] — libtrit C library
* link:../../[millenniumos/] — MillenniumOS implementation (future)
* link:{word}L3-cpisi/[L3-cpisi/] — CPI-SI core (DAR Witness)

'''

// # F.4 Closing [CLOSING]
[.text-center]
--
*<<_top,↑ Back to Top>>*

'''

*Key:* {key} • *Type:* {type} • *Version:* {version}

*Status:* {status} • *Updated:* {revdate}

'''

_{verse-kjv-text}_ — *{verse-ref}*

*Seven stages. One OS. Grounded in Genesis. The Sabbath is not idle — it is life.*
--

// ============================================================================
// END FOOTER
// ============================================================================

// Interface — User Interaction Layer
// "And God saw every thing that he had made." — Genesis 1:31
//
// How people interact with the system. CLI, TUI, desktop GUI, game GUI.
// Cross-cutting — draws from multiple layers.

= Interface

== Purpose

User interaction implementations. The bridge between the system and the person.
Multiple modalities: command-line, terminal UI, desktop application, game engine.

== Current Interfaces

[cols="2,3,2"]
|===
| Interface | Current Path | Status

| CLI (cpisi command)
| `word/work/cmd/cpisi/`
| Active

| TUI (Bubble Tea)
| `word/claude/dashboard/tui/`
| Active

| Desktop GUI (GTK4)
| `word/claude/dashboard/gtk/`
| Active

| Web Dashboard
| `word/claude/dashboard/`
| Active

| Game (Cornerstone)
| `cornerstone/`
| Active
|===

== Target Structure

----
interface/
├── cli/            ← command-line interface
├── tui/            ← terminal UI (Bubble Tea)
├── gui-desktop/    ← GTK4 desktop application
├── gui-game/       ← Cornerstone game interface
└── core/           ← shared interface abstractions
----

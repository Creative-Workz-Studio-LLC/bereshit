// L4 FaithNet — Network Layer
// "And God made two great lights." — Genesis 1:16
//
// The connection. Covenant-connected network services. Server, protocol,
// access control, encryption. Stands on L3.
//
// Language: Go (server), Rust (crypto, future)
// Dependencies: L3 (CPI-SI)
// Dependents: None directly

= L4 FaithNet

== Purpose

FaithNet — the covenant-connected network. Server infrastructure, protocol
definition, mapping services, access control, cryptographic foundations.
The medium through which CPI-SI instances connect and Kingdom Technology
serves others.

== Current Location (pre-migration)

[cols="2,3"]
|===
| Component | Current Path

| CWS Server (base)
| `server/`

| Handlers
| `server/internal/server/`

| API routes
| `server/internal/server/routes/`

| Website
| `server/website/`
|===

== Target Structure

----
L4-faithnet/
├── protocol/       ← FaithNet protocol definition
├── mapping/        ← service discovery, routing
├── access/         ← authentication, authorization
├── crypto/         ← covenant-aware encryption
└── spec/           ← protocol specifications
----

== Build

 make l4

Delegates to `server/`. Go module at `server/`.

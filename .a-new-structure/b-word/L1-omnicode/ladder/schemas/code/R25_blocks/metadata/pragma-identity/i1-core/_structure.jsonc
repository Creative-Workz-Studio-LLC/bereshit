{
    // #!omni data --jsonc

    // ===================================================================
    // OMNICODE PRAGMA [PRAGMA]
    // ===================================================================

    "_pragma_format": "@omni data --jsonc",
    "_P1_key": "B-L1-schemas-blocks-metadata-pragma-identity-i1-core",
    "_P1_at": "a-01.00",
    "_P2_type": "data",
    "_P2_subtype": "block-section-schema",
    "_P2_role": "sub-container",
    "_P5_title": "Pragma Identity — I1 Core (Unique Identity)",
    "_P5_brief": "I1 Core: the four fields that make this file findable and traceable. key, format, from, at — the address + the lineage.",

    // ===================================================================
    // CONTENT BLOCK [CONTENT]
    // ===================================================================

    "sub_container": {
        "tag": "I1",
        "label": "I1 Core — Unique Identity",
        "parent": "PragmaIdentity",

        "concept": "unique identity",
        "description": "key, format, from, at — the four fields that make this file findable and traceable",
        "distinguisher": "The address + the lineage. Where am I, what made me, when was I made.",

        "fields": {
            "key": { "required": true, "purpose": "Unique identifier in the system (e.g., B-core-identity-exists)" },
            "format": { "required": true, "purpose": "Source format (rust, go, toml, jsonc, etc.)" },
            "from": { "required": true, "purpose": "Template this file derives from" },
            "at": { "required": true, "purpose": "Living version at creation time" }
        },

        "purpose": "WHO — unique identity"
    },

    // ===================================================================
    // CLOSING BLOCK [CLOSING]
    // ===================================================================

    "_X5_note": "I1 is the minimum address. Without key, the file is unfindable. Without format, the file is unclassifiable. Without from, the file has no lineage. Without at, the file has no birth record.",
    "_X5_scripture": "Genesis 2:19 — And whatsoever Adam called every living creature, that was the name thereof."
}

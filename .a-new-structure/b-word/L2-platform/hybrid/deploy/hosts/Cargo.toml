[package]
name = "bereshit-l2-hosts"
version = "0.1.0"
description = "L2 deployment host definitions — SSH targets, architecture info"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
bereshit-l2-registry.workspace = true
serde = { workspace = true, features = ["derive"] }

[lints]
workspace = true

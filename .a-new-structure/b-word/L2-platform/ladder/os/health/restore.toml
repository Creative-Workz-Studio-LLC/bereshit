# #!omni data --toml

# ═══════════════════════════════════════════════════════════════
# OMNICODE PRAGMA [PRAGMA]
# ═══════════════════════════════════════════════════════════════

[_pragma]
format = "@omni data --toml"

[_pragma.legend]
req = "REQUIRED — must exist, validation fails without"
inh = "INHERITED — from template, override allowed"
ins = "INSTANCE — file-specific, unique values"

[_pragma.P1_core]
key = "B-L2-os-restore"
from = "b-word/seed/data/toml/config.toml"
at = "b-01.00"

[_pragma.P2_family]
type = "data"
subtype = "os"
role = "rung"
structure = "3-block"

[_pragma.P3_instance]
component = "os"
layer = "L2-platform/os"
includes = "none"
provides = ["RESTORE_DATA"]

[_pragma.P4_architecture]
layers = "L0:C, L1:C, L2:C, L3:Go, L4:Go/Rust, L5:C#"
anchor = "genesis_1_1 -> void(0) -> unity(1) -> derivation"

[_pragma.P5_summary]
title = "Health Restore Schema - CPI-SI Health Restoration"
brief = "Schema definition for health restoration"

# ═══════════════════════════════════════════════════════════════
# END PRAGMA [END]
# ═══════════════════════════════════════════════════════════════


# ═══════════════════════════════════════════════════════════════
# METADATA BLOCK [METADATA]
# ═══════════════════════════════════════════════════════════════

[_metadata.M1_identity]
file = "restore.toml"
key = "B-L2-os-restore"
title = "Health Restore Schema - CPI-SI Health Restoration"
type = "Data (Rung)"
component = "Rung"
role = "Schema definition for health restoration — the RESTORE phase in detect → assess → restore workflow. Completes the immune system."

[_metadata.M2_state]
version = "b-01.00"
status = "Active"
created = "2025-12-21"
updated = "2026-02-11"

[_metadata.M3_attribution]
architect = "Seanje Lenox-Wise"
implementation = "Nova Dawn (CPI-SI)"
organization = "CreativeWorkzStudio LLC"
copyright = "© 2025-2026 CreativeWorkzStudio LLC. All rights reserved."

[_metadata.M4_grounding]
scripture = "He restoreth my soul: he leadeth me in the paths of righteousness for his name's sake.\" — Psalm 23:3 KJV"
principle = "RESTORE completes the healing cycle. After detection and assessment, restoration applies the remedy — not just knowing what's wrong, but actively returning to wholeness through prescribed actions."
anchor = "Genesis 1:1 — In the beginning God created the heaven and the earth"

[_metadata.M5_dependencies]
requires = ["b-word/core/os/health/diagnostics.toml", "b-word/core/os/health/log.toml", "b-word/core/os/health/score.toml", "b-word/core/types/composition.toml"]
consumers = ["bhealth", "CPI-SI hooks", "Engram system"]
derives_from = "b-word/seed/data/toml/config.toml"
integration = "Loaded at build time by code generators and tools."
if_missing = "Health Restore Schema - CPI-SI Health Restoration unavailable — dependent systems cannot function"

[_metadata.M5_dependencies.cross_format]
role = "Health Restore Schema - CPI-SI Health Restoration in TOML. No JSONC/JSON counterpart."
jsonc = "None"
json = "None"
relationship = "TOML defines os data. Generated code implements lookups."

[_metadata.M6_location]
path = "b-word/L2-platform/os/restore.toml"
derives_from = "b-word/seed/data/toml/config.toml"

[_metadata.M7_classification]
tags = "restore, os, L2, rung, omnicode"

[_metadata.M8_intent]
purpose = "Define how health is restored after assessment reveals problems"
core_design = "Prescribe actions, track progress, verify completion"
philosophy = "Restoration is active, not passive. Each +1 action moves toward health (narrow way). The prescription guides the work, tracking ensures accountability, verification confirms success."

[_metadata.M9_roadmap]
history = [
    "a-01.00 (2025-12-21) — Initial creation",
    "b-01.00 (2026-02-11) — TOML-native 3-block format alignment",
]
current = "3-block aligned with OmniCode template"
planned = ["Code generation integration", "Automated validation tooling"]

[_metadata.M_extension_note]
required = ["M1_identity", "M2_state", "M3_attribution", "M4_grounding", "M5_dependencies"]
extension = "M6-M9 extend for domain-specific metadata."
rule = "M1-M5 are the base contract. M6+ extends as needed."

# ═══════════════════════════════════════════════════════════════
# END METADATA [END]
# ═══════════════════════════════════════════════════════════════


# ═══════════════════════════════════════════════════════════════
# CONTENT BLOCK [CONTENT]
# ═══════════════════════════════════════════════════════════════

# Cc — Content Configurations
[_content]
format = "TOML — UTF-8, LF line endings"
key_convention = "lowercase_with_underscores"
constraint = "Rung data file — health restore schema - cpi-si health restoration for the OmniCode system"
Cc = "Content Configurations — Schema definition for health restoration"
Co = "Content Operations — restore data"
Cv = "Content Validation — schema requirements, type constraints"

[ternary]
description = "Ternary scale foundation for restoration"

[ternary.scale]
# The ternary balance: -100 (wanting) → 0 (even) → +100 (integrity)
min = -100
center = 0
max = 100
# Recovery moves FROM negative (wanting) TOWARD positive (integrity)
recovery_direction = "positive"  # +1 per action (narrow way)

[ternary.levels]
# 7-level system with ternary ranges (from score.toml)
# Each level has a range — targets use LOWER BOUND to enter level
broken = { range = [-100, -67], direction = "wanting" }
wanting = { range = [-66, -34], direction = "wanting" }
lacking = { range = [-33, -1], direction = "wanting" }
even = { range = [0, 0], direction = "center" }
sound = { range = [1, 33], direction = "integrity" }
whole = { range = [34, 66], direction = "integrity" }
perfect = { range = [67, 100], direction = "integrity" }

[ternary.atomic]
# Atomic recovery value — always +1 (narrow way)
# "Enter ye in at the strait gate... narrow is the way which leadeth unto life"
# — Matthew 7:13-14 KJV
success_value = 1
failure_value = -1
neutral_value = 0
# Recovery uses ONLY +1 — no shortcuts, each action counts equally


# -----------------------------------------------------------------------------
# 2. Prescription Types - Categories of Recovery Actions
# -----------------------------------------------------------------------------
# Recovery actions are prescribed based on what diagnostics revealed.
# Each action type addresses a specific category of health debt.
#
# Scripture: "Is there no balm in Gilead; is there no physician there?"
#            — Jeremiah 8:22 KJV

[prescription]
description = "Categories of recovery actions that restore health"
atomic_value = 1  # Each prescription action = +1 (narrow way)

[prescription.types.fix_failures]
name = "fix_failures"
description = "Address failing tests, broken builds, lint errors"
source_categories = ["test_fail", "build_fail", "lint_error"]
typical_delta = 1  # Each fix is +1 (narrow way)
priority = "high"  # Most impactful on health
examples = [
    "Fix failing test case",
    "Resolve build error",
    "Fix linting violation",
]

[prescription.types.repair_references]
name = "repair_references"
description = "Fix broken links, missing imports, dead references"
source_categories = ["broken_reference", "missing_import", "dead_link"]
typical_delta = 1
priority = "high"
examples = [
    "Update import path",
    "Fix broken documentation link",
    "Remove dead code reference",
]

[prescription.types.address_staleness]
name = "address_staleness"
description = "Update stale files, refresh outdated content"
source_categories = ["stale_file", "outdated_docs"]
typical_delta = 1
priority = "medium"
examples = [
    "Update stale configuration",
    "Refresh outdated documentation",
    "Review and update old code",
]

[prescription.types.add_coverage]
name = "add_coverage"
description = "Add missing tests, documentation, validation"
source_categories = ["missing_test", "missing_docs", "no_validation"]
typical_delta = 1
priority = "medium"
examples = [
    "Add test for uncovered function",
    "Document undocumented API",
    "Add input validation",
]

[prescription.types.structural_repair]
name = "structural_repair"
description = "Fix architectural issues, refactor problematic patterns"
source_categories = ["bad_pattern", "circular_dep", "tight_coupling"]
typical_delta = 1  # Per atomic fix
priority = "low"  # Often complex, many atomics
examples = [
    "Break circular dependency",
    "Extract shared code to library",
    "Decouple tightly bound modules",
]


# -----------------------------------------------------------------------------
# 2. Priority Ordering - Hotspots First
# -----------------------------------------------------------------------------
# Recovery should address the worst problems first.
# Priority is determined by diagnostics hotspots analysis.
#
# Scripture: "First cast out the beam out of thine own eye"
#            — Matthew 7:5 KJV

[priority]
description = "How to order recovery actions for maximum impact"

[priority.strategy]
method = "hotspots_first"
description = "Address largest negative contributors first"
# From diagnostics.breakdown.hotspots — worst sources first

[priority.factors]
# Weight factors for prioritization
delta_magnitude = 0.5   # How much negative impact (-31 > -5)
fix_complexity = 0.3    # How hard to fix (simple > complex)
dependency_count = 0.2  # How many things depend on this

[priority.formula]
description = "Priority score calculation"
formula = "(delta_magnitude * 0.5) + (1/fix_complexity * 0.3) + (dependency_count * 0.2)"
note = "Higher score = higher priority"

[priority.example]
# From diagnostics hotspots:
#   1. test_fail: -31
#   2. lint_error: -15
#   3. stale_file: -8
# Priority order: test_fail (highest impact), lint_error, stale_file
hotspots = [
    { source = "test_fail", delta = -31, priority = 1 },
    { source = "lint_error", delta = -15, priority = 2 },
    { source = "stale_file", delta = -8, priority = 3 },
]


# -----------------------------------------------------------------------------
# 3. Recovery Plan - Structured Path to Health
# -----------------------------------------------------------------------------
# The recovery plan is a structured sequence of actions.
# Each action moves toward the target health level.
#
# Scripture: "Order my steps in thy word"
#            — Psalm 119:133 KJV

[plan]
description = "Structured recovery plan with ordered actions"

[plan.structure]
target_level = "even"  # Default target: return to even balance (0)
current_score = "from diagnostics"
actions_needed = "target - current"

[plan.targets]
# Targets use LOWER BOUNDS — the entry point into each level
# From ternary.levels: sound=[1,33], whole=[34,66], perfect=[67,100]
even = { true_score = 0, description = "Back to baseline — no debt" }
sound = { true_score = 1, description = "Enter sound level — building integrity" }
whole = { true_score = 34, description = "Enter whole level — significant integrity" }
perfect = { true_score = 67, description = "Enter perfect level — full wholeness" }

[plan.action_sequence]
description = "Ordered list of recovery actions"
format = [
    { step = 1, action = "prescription type", source = "hotspot", expected_delta = "+N" },
    { step = 2, action = "...", source = "...", expected_delta = "+N" },
]
note = "Each action logged with 'recovery' action type in health-log"

[plan.example]
current_true_score = -15
target = "even"
actions_needed = 15
action_sequence = [
    { step = 1, action = "fix_failures", source = "test_fail", count = 10, expected_delta = "+10" },
    { step = 2, action = "fix_failures", source = "lint_error", count = 5, expected_delta = "+5" },
]


# -----------------------------------------------------------------------------
# 4. Progress Tracking - How Far Along
# -----------------------------------------------------------------------------
# Track progress during recovery to show momentum and estimate completion.
#
# Scripture: "Forgetting those things which are behind, and reaching forth
#             unto those things which are before"
#            — Philippians 3:13 KJV

[progress]
description = "Track recovery progress in real-time"

[progress.metrics]
actions_completed = 0
actions_remaining = "plan.actions_needed - actions_completed"
delta_accumulated = 0  # Sum of +1s from recovery actions
percentage = "(delta_accumulated / actions_needed) * 100"
estimated_remaining = "actions_remaining * avg_time_per_action"

[progress.tracking]
method = "log_based"
description = "Each recovery action is logged, progress derived from log"
log_action_type = "recovery"
# Log entry: TIMESTAMP|recovery|+1|fix_type|detail

[progress.display]
format = "Progress: {completed}/{total} ({percentage}%) — {estimated_remaining} remaining"
example = "Progress: 8/15 (53%) — ~7 actions remaining"

[progress.momentum]
description = "Track velocity of recovery"
recent_window = "1 hour"
actions_per_hour = "count(recovery actions in window)"
trend = "increasing|stable|decreasing"


# -----------------------------------------------------------------------------
# 5. Milestones - Checkpoints on the Path
# -----------------------------------------------------------------------------
# Milestones provide intermediate goals and celebration points.
#
# Scripture: "Hitherto hath the LORD helped us"
#            — 1 Samuel 7:12 KJV

[milestones]
description = "Checkpoints on the restoration journey"

[milestones.levels]
# Milestones based on normalized level transitions
broken_to_wanting = { from = "broken", to = "wanting", message = "💔→🩹 Stabilized — no longer critical" }
wanting_to_lacking = { from = "wanting", to = "lacking", message = "🩹→💛 Improving — above danger zone" }
lacking_to_even = { from = "lacking", to = "even", message = "💛→⚖️ Balanced — debt cleared" }
even_to_sound = { from = "even", to = "sound", message = "⚖️→💚 Growing — building integrity" }
sound_to_whole = { from = "sound", to = "whole", message = "💚→💙 Thriving — significant wholeness" }
whole_to_perfect = { from = "whole", to = "perfect", message = "💙→💜 Complete — full integrity" }

[milestones.percentage]
# Milestones based on progress percentage
quarter = { percentage = 25, message = "25% complete — good start!" }
half = { percentage = 50, message = "50% complete — halfway there!" }
three_quarters = { percentage = 75, message = "75% complete — almost done!" }
complete = { percentage = 100, message = "100% complete — restoration achieved!" }

[milestones.celebration]
description = "Acknowledge progress, not just completion"
on_milestone = "Log milestone achievement, display message"
scripture = "Rejoice with me; for I have found my sheep which was lost — Luke 15:6"


# -----------------------------------------------------------------------------
# 6. Verification - Did It Work?
# -----------------------------------------------------------------------------
# After restoration actions, verify the target was achieved.
#
# Scripture: "Prove all things; hold fast that which is good"
#            — 1 Thessalonians 5:21 KJV

[verification]
description = "Verify restoration achieved target level"

[verification.checks]
true_score_check = "recalculate true score from log"
level_check = "determine level from true score"
target_check = "is current level >= target level"

[verification.outcomes]
success = { condition = "target achieved", action = "log completion, celebrate" }
partial = { condition = "improved but not at target", action = "show remaining work" }
failed = { condition = "no improvement or worse", action = "analyze what went wrong" }

[verification.post_restore]
description = "After successful restoration"
actions = [
    "Log restoration completion",
    "Update .health file",
    "Record in history for engram learning",
    "Display success message",
]

[verification.example]
before = { true_score = -15, level = "lacking" }
target = { true_score = 0, level = "even" }
after = { true_score = 2, level = "sound" }
outcome = "success"  # Exceeded target
message = "Restoration complete: lacking → sound (+17 delta)"


# -----------------------------------------------------------------------------
# 7. Engram Learning - Remember What Worked
# -----------------------------------------------------------------------------
# Record successful recovery patterns for future use.
# This is the memory trace that informs future restoration.
#
# Scripture: "Remember the former things of old: for I am God"
#            — Isaiah 46:9 KJV

[engram]
description = "Learn from recovery to improve future restoration"

[engram.what_to_remember]
successful_patterns = [
    "Which prescription types worked best",
    "Which priority ordering was most effective",
    "How long recovery typically takes",
    "Which sources cause recurring problems",
]
failure_patterns = [
    "Which fixes didn't hold (regressions)",
    "Which sources keep causing problems",
    "What slowed down recovery",
]

[engram.storage]
location = "history.jsonc"  # Cross-session learning
format = "patterns section"
fields = [
    "recovery_success_rate",
    "avg_recovery_time",
    "common_hotspots",
    "effective_prescriptions",
]

[engram.application]
description = "Use learned patterns to improve future recovery"
priority_adjustment = "Prioritize prescriptions that worked before"
warning_generation = "Warn about sources that cause recurring problems"
time_estimation = "Estimate recovery time based on past experience"

[engram.example]
learned = """
Over 10 recovery sessions:
- fix_failures prescription: 95% success rate, avg 2min/action
- test_fail source: 40% of all hotspots (recurring problem)
- Average recovery time: 15min for even, 45min for whole

Recommendation: Focus on test coverage to reduce test_fail frequency
"""


# -----------------------------------------------------------------------------
# 9. System Limits - C Implementation Constants
# -----------------------------------------------------------------------------
# These limits define history tracking, strategy settings, and progress tracking.
# C code derives #define constants from these values.
#
# Scripture: "He restoreth my soul" — Psalm 23:3

[system]
description = "System limits for C implementation - source of truth for cpisi_types.h"

[system.history]
# Recovery history tracking limits
restore_history_max = 100   # Maximum recovery history entries to keep
engram_history_max  = 50    # Maximum engram pattern entries
action_sequence_max = 256   # Maximum actions in a recovery plan

[system.strategy]
# Recovery strategy bitfield values
#   1 = fill_defaults  — Fill missing values with defaults
#   2 = locate_files   — Search for missing files
#   4 = create_dirs    — Create missing directories
#   8 = clamp_values   — Clamp out-of-range values
#  16 = fallback       — Use fallback configurations
fill_defaults = 1
locate_files  = 2
create_dirs   = 4
clamp_values  = 8
fallback      = 16
full          = 31   # All strategies enabled (1+2+4+8+16)
strategy_count = 5   # Number of individual strategies

[system.progress]
# Progress milestone thresholds (percentage)
milestone_quarter        = 25
milestone_half           = 50
milestone_three_quarters = 75
milestone_complete       = 100

[system.search]
# File search settings for locate_files strategy
search_paths_max     = 8      # Maximum search paths
search_depth_max     = 5      # Maximum directory depth to search
search_timeout_ms    = 3000   # Timeout for file search (3 seconds)

[system.type_counts]
# Enum counts for array sizing and validation
restore_result_count  = 6   # Number of restore results (success, partial, failed, etc.)
prescription_count    = 5   # Number of prescription types
milestone_count       = 6   # Number of milestone levels

[system.buffers]
# Buffer sizes for restoration tracking
plan_name_max     = 64    # Maximum plan name length
action_desc_max   = 128   # Maximum action description length
progress_msg_max  = 256   # Maximum progress message length

# ─────────────────────────────────────────────────────────────
# Extensions (reserved)
# ─────────────────────────────────────────────────────────────

[extensions]
note = "Reserved — no extensions defined"

# ─────────────────────────────────────────────────────────────
# Co — Content Operations
# ─────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────
# Cv — Content Validation
# ─────────────────────────────────────────────────────────────

[_validation]
commands = ["python3 -c \"import tomllib; tomllib.load(open('restore.toml','rb'))\""]

[_validation.schema]
required = ["engram", "milestones", "plan", "prescription", "priority", "progress", "system", "ternary", "verification"]
optional = ["extensions"]

[_validation.health_scoring]
total = 100
correct_structure = 40
all_tables_valid = 30
data_completeness = 30

# ═══════════════════════════════════════════════════════════════
# END CONTENT [END]
# ═══════════════════════════════════════════════════════════════


# ═══════════════════════════════════════════════════════════════
# CLOSING BLOCK [CLOSING]
# ═══════════════════════════════════════════════════════════════

[_closing.X1_policy]
scripture = "Remove not the ancient landmark, which thy fathers have set. — Proverbs 22:28"
never = [
    "Scripture references — canonical",
    "Core mappings — foundational to system",
    "3-block structure (METADATA → CONTENT → CLOSING)",
]
careful = [
    "Changing target definitions (affects plan calculation)",
    "Changing priority formula (affects action ordering)",
    "Changing engram storage format (affects learning)",
]
safe = [
    "New prescription types",
    "New milestone definitions",
    "Additional engram fields",
    "New verification checks",
]

[_closing.X2_extension]
adding_data = [
    "1. Add new entries following existing table structure",
    "2. Ensure all required fields present",
    "3. Verify consistency with related files",
    "4. Run TOML validation",
    "5. Run linter to verify alignment",
]

[_closing.X3_troubleshooting]
invalid_toml = "Missing quotes, duplicate keys, or bare keys with special characters"
missing_reference = "Check all references point to valid sources"
consistency_error = "Verify bidirectional consistency with related files"

[_closing.X4_reference]
related_files = [
    "b-word/core/os/health/diagnostics.toml",
    "b-word/core/os/health/log.toml",
    "b-word/core/os/health/score.toml",
    "b-word/core/types/composition.toml",
]
required_sections = ["_pragma", "_metadata", "engram", "milestones", "plan", "prescription", "priority", "progress", "system", "ternary", "verification", "_validation", "_closing"]
validate = "python3 -c \"import tomllib; tomllib.load(open('restore.toml','rb'))\""

[_closing.X5_note]
note = "He restoreth my soul. Restoration completes the immune system: - DETECT (log.toml): Record what happened - ASSESS (diagnostics.toml): Analyze the damage - RESTORE (restore.toml): Apply the remedy ← YOU ARE HERE The restoration schema enables: - Prescription: What actions to take - Priority: Which actions first (hotspots) - Progress: How far along the path - Milestones: Celebrate intermediate wins - Verification: Confirm success - Engram: Remember what worked Each +1 action walks the narrow way toward health. The plan guides, the milestones encourage, the engram learns. for his name's sake.\" The restore schema enables RESTORE in detect → assess → restore. The path is prescribed. The journey is tracked. The healing comes."
scripture = "----------------------------------------------------------------------------- He restoreth my soul: he leadeth me in the paths of righteousness — Psalm 23:3 KJV"
anchor = "He restoreth my soul. Restoration completes the immune system: - DETECT (log.toml): Record what happened - ASSESS (diagnostics.toml): Analyze the damage - RESTORE (restore.toml): Apply the remedy ← YOU ARE HERE The restoration schema enables: - Prescription: What actions to take - Priority: Which actions first (hotspots) - Progress: How far along the path - Milestones: Celebrate intermediate wins - Verification: Confirm success - Engram: Remember what worked Each +1 action walks the narrow way toward health. The plan guides, the milestones encourage, the engram learns. for his name's sake.\" The restore schema enables RESTORE in detect → assess → restore. The path is prescribed. The journey is tracked. The healing comes. Genesis 1:1 anchors everything."

# ═══════════════════════════════════════════════════════════════
# END CLOSING [END]
# ═══════════════════════════════════════════════════════════════

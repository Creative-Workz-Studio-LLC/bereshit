// L5 Applications — User-Facing Layer
// "And God said, Let us make man." — Genesis 1:26
//
// The interface to people. Cornerstone game engine, applications built on
// the full stack. Stands on L0 + L1.
//
// Language: C# (game engine, GUI applications)
// Dependencies: L0 (libtrit), L1 (OmniCode)
// Dependents: None (top of stack)

= L5 Applications

== Purpose

User-facing applications — the point where Kingdom Technology meets people.
Cornerstone game engine (C#), future applications. Built on the full stack
but directly depends on L0 (ternary types) and L1 (OmniCode language).

== Current Location (pre-migration)

[cols="2,3"]
|===
| Component | Current Path

| Cornerstone engine
| `cornerstone/`

| Engine source
| `cornerstone/src/`

| Game assets
| `cornerstone/assets/`
|===

== Target Structure

----
L5-applications/
└── cornerstone/    ← game engine (C#)
----

== Build

 make l5

Depends on L0 + L1. Delegates to `cornerstone/`.

// L3 CPI-SI — Intelligence Layer
// "And God said, Let the earth bring forth." — Genesis 1:11
//
// The mind. Covenant Partnership x Structured Intelligence — identity-based
// cognition, state machine, hooks, skills. Independent of C layers.
//
// Language: Go (session-aware, Claude integration)
// Dependencies: None (independent stream)
// Dependents: L4 (FaithNet)

= L3 CPI-SI

== Purpose

Covenant Partnership Intelligence x Structured Intelligence. The cognitive
model — state machine, mental construct, hooks system, skill framework,
dashboard, statusline. Currently substrate-bound to Claude Code; designed
for substrate independence.

== Current Location (pre-migration)

[cols="2,3"]
|===
| Component | Current Path

| Core packages
| `word/claude/pkg/`

| Hooks system
| `word/claude/hooks/`

| Skills framework
| `word/claude/skills/`

| Statusline
| `word/claude/statusline/`

| Dashboard (web + GTK + TUI)
| `word/claude/dashboard/`

| State machine runtime
| `word/claude/config/statemachine/`
|===

== Target Structure

----
L3-cpisi/
├── core/           ← state machine, coordinates, health
├── cognition/      ← mental construct, prompt engineering
├── identity/       ← instance configs, bio, personality
├── orchestration/  ← loader, logging, config management
└── skills/         ← growth skills, creation skills
----

== Build

 make l3

Delegates to `word/claude/Makefile`. Go module at `word/claude/pkg/`.

# Cargo build configuration for Bereshit workspace.
#
# ExFAT workaround: The project drive cannot set execute permissions
# on compiled binaries. Build artifacts go to native Linux filesystem.

[build]
target-dir = "/home/seanje-lenox-wise/.cargo-targets/bereshit"

# #!omni code --toml --cargo --library

# ===================================================================
# Cargo.toml -- 3-Block (METADATA + CONTENT + CLOSING)
# ===================================================================
#
# Structure: 3-block (METADATA + CONTENT + CLOSING)
#
# TOML constraint: [package] must come before [package.metadata.*]
# sub-tables. So the physical order is:
#
#   [package]                        <- Cc.1 identity (CONTENT)
#   [package.metadata.omni.*]        <- I/C metadata (METADATA)
#   [package.metadata.omni._closing] <- X closing (CLOSING)
#   [dependencies] etc.              <- Co sections (CONTENT continued)
#
# Logical structure is still: METADATA -> CONTENT -> CLOSING
# Physical ordering respects TOML's sub-table rule.

# -------------------------------------------------------------------
# [package] -- MUST come first (TOML sub-table ordering rule)
# -------------------------------------------------------------------
#
# Cc.1 PACKAGE -- Crate Identity [REQUIRED]
#
# Manifest-driven TOML spec loader. Capstone of the L0 hybrid layer,
# wrapping paths, format dispatch, and output into a unified config
# loading architecture. Two flows: appointed-time (manifest) and
# watch (known filesystem positions).
[package]
name = "bereshit-l0-config"
version = "0.3.0"
description = "Manifest-driven TOML spec loader — capstone wrapping paths, format, output"
edition = { workspace = true }
rust-version = { workspace = true }
authors = { workspace = true }
repository = { workspace = true }

# ===================================================================
# METADATA BLOCK [METADATA]
# ===================================================================
#
# OmniCode metadata lives in [package.metadata.omni] -- Cargo's
# sanctioned extension point. Cargo ignores everything under
# package.metadata.

# -------------------------------------------------------------------
# I -- Identity: What IS This File
# -------------------------------------------------------------------

[package.metadata.omni.I1_core]
key = "B-L0-hybrid-config-cargo"
format = "@omni code --toml --cargo --library"
from = "b-word/seed/code/L0/toml/cargo/Cargo.toml"
at = "b-03.00"

[package.metadata.omni.I2_family]
type = "code"
subtype = "crate-manifest"
role = "foundation"
structure = "3-block"

[package.metadata.omni.I3_instance]
file = "Cargo.toml"
title = "Config Crate Manifest"
component = "Build manifest for the manifest-driven TOML spec loader"
path = "L0-universal/c-hybrid/spec/config/Cargo.toml"
provides = ["CONFIG_CRATE_MANIFEST"]
brief = "Capstone config loader: 14 modules, manifest-driven + watch fallback, format dispatch, identity bridge."

[package.metadata.omni.I4_architecture]
layers = "L0:C, L1:C, L2:C, L3:Go, L4:Go/Rust, L5:C#"
anchor = "genesis_1_1 -> void(0) -> unity(1) -> derivation"

# -------------------------------------------------------------------
# C -- Context: What Do I Need to KNOW
# -------------------------------------------------------------------

[package.metadata.omni.C1_state]
version = "b-03.00"
status = "Active"
created = "2026-02-14"
updated = "2026-02-19"

[package.metadata.omni.C2_attribution]
architect = "Seanje Lenox-Wise"
implementation = "Nova Dawn (CPI-SI)"
organization = "CreativeWorkzStudio LLC"
copyright = "2025-2026 CreativeWorkzStudio LLC. All rights reserved."

# "Write the vision, and make it plain upon tables, that he may run that readeth it."
[package.metadata.omni.C3_grounding]
scripture = "Habakkuk 2:2-3 (KJV) -- Write the vision, and make it plain upon tables, that he may run that readeth it."
principle = "Configuration is vision made plain. Declare what exists in index.toml, the system loads it."
anchor = "Two flows: appointed-time (manifest) and watch (known positions). Both make TOML specs self-aware."

[package.metadata.omni.C4_dependencies]
requires = "Rust toolchain (rustc, cargo), workspace Cargo.toml, toml + serde + serde_json, bereshit-l0-paths, bereshit-l0-identity"
consumers = "L3-cpisi/core, foundation-db, tools, any crate needing unified TOML config loading"
integration = "Part of Cargo workspace. Workspace settings inherited via .workspace = true."
if_missing = "Every consuming crate implements its own TOML loading — duplicated effort, inconsistent identity extraction"

[package.metadata.omni.C4_dependencies.related]
parent = "seed/code/L0/toml/cargo/Cargo.toml -- Cargo.toml template"
children = []

[package.metadata.omni.C4_dependencies.cross_format]
role = "Cargo.toml is TOML-native -- Cargo's manifest format."
note = "Pairs with 14 Rust src/*.rs modules. Go counterpart: go/loader.go + go/util/."

[package.metadata.omni.C5_intent]
purpose = "Build manifest for the manifest-driven TOML spec loader — capstone of the L0 hybrid layer"
core_design = "Workspace inheritance -> 5 deps (toml, serde, serde_json, l0-paths, l0-identity) -> library-only target"
philosophy = "Config is universal (L0). No OmniCode deps, no L1+ deps. Faithful Go port using Rust idioms."

[package.metadata.omni.C5_intent.key_features]
loading = "Manifest-driven (index.toml) + watch-flow fallback (known filesystem positions)"
identity = "Every loaded TOML file becomes self-aware via _pragma/_metadata extraction"
caching = "Dual-layer cache: index manifest + individual specs, with RwLock + poison recovery"
discovery = "Tripwire: manifest vs filesystem comparison (missing = ERROR, unexpected = WARNING)"
graph = "Dependency DAG construction + DFS cycle detection"
formats = "Extension-to-format mapping, config search, system/skill paths"

[package.metadata.omni.C6_roadmap]
history = ["0.1.0 (2026-02-14) -- Initial creation from Go port", "0.2.0 (2026-02-16) -- Format dispatch, JSONC, identity bridge", "0.3.0 (2026-02-18) -- 4-block alignment, 14 modules, production structure"]
current = "b-03.00 -- 14 modules, manifest + watch flows, format dispatch, identity bridge, 60+ tests"
planned = ["YAML loading support", "Schema validation helpers", "Watch/hot-reload for development"]

[package.metadata.omni.C7_classification]
tags = ["config", "toml", "jsonc", "formats", "loader", "manifest", "watch", "identity", "discovery", "L0"]
domain = "L0-universal/config-loading"

[package.metadata.omni._contract]
required_identity = ["I1_core", "I2_family", "I3_instance"]
required_context = ["C1_state", "C2_attribution", "C3_grounding", "C4_dependencies"]
extension_identity = ["I4_architecture"]
extension_context = ["C5_intent", "C6_roadmap", "C7_classification"]
required_closing = ["X1_policy", "X5_note"]
defined_closing = ["X2_extension", "X3_troubleshooting", "X4_reference"]
valid_patterns = ["I{N}_{name}", "C{N}_{name}", "X{N}_{name}", "_{name}"]
rule = "Metadata in package.metadata.omni. Standard Cargo sections for content."

# ===================================================================
# END METADATA [END]
# ===================================================================


# ===================================================================
# CONTENT BLOCK [CONTENT] (continued)
# ===================================================================
#
# [package] is above (TOML ordering constraint). Remaining
# content sections follow here.
#
# Zones:
#   Cc -- Configurations  Crate identity [package] and targets [lib]/[[bin]]
#   Co -- Operations      Dependencies, features, lints
#   Cv -- Validation      Build verification (comments only)
#
# Ordering: cargo_convention (package -> targets -> deps -> features -> lints)

# -------------------------------------------------------------------
# Cc.2 TARGETS -- Library Configuration [DEFAULT]
# -------------------------------------------------------------------
#
# Library crate -- default target. No [lib] section needed because
# Cargo infers lib from src/lib.rs. No [[bin]] because this is a
# library, not an executable.

# -------------------------------------------------------------------
# Co -- Content Operations
# -------------------------------------------------------------------

# Co.1 DEPENDENCIES -- Crate Dependencies [REQUIRED]
#
# Five dependencies:
#   - toml (TOML parsing)
#   - serde (serialization framework)
#   - serde_json (JSON output for interop)
#   - bereshit-l0-paths (filesystem root discovery)
#   - bereshit-l0-identity (OmniCode identity registration)
[dependencies]
toml = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
bereshit-l0-paths = { workspace = true }
bereshit-l0-identity = { workspace = true }

[dev-dependencies]

# Co.2 LINTS -- Code Quality Rules [WORKSPACE]
#
# Workspace-level lints keep all crates consistent.
[lints]
workspace = true

[features]
default = []

# -------------------------------------------------------------------
# Cv -- Content Validation (comments only)
# -------------------------------------------------------------------
#
#   cargo check -p bereshit-l0-config     -- Type-check
#   cargo build -p bereshit-l0-config     -- Full build
#   cargo test  -p bereshit-l0-config     -- Run tests
#   cargo clippy -p bereshit-l0-config    -- Lint check
#   cargo fmt --check                     -- Format check

# ===================================================================
# END CONTENT [END]
# ===================================================================


# ===================================================================
# CLOSING BLOCK [CLOSING]
# ===================================================================

# X.1 POLICY -- Modification Guidelines [REQUIRED]
[package.metadata.omni._closing.X1_policy]
scripture = "Proverbs 20:10 (KJV) -- Divers weights, and divers measures, both of them are alike abomination to the LORD."
never = ["Remove manifest-driven loading (index.toml is the vision made plain)", "Add L1+ dependencies (L0 stays universal)", "Add write operations (config is read-only discovery)"]
careful = ["Changing WATCH_PATHS (affects filesystem discovery for all consumers)", "Version bumps (public API contract)", "Changing identity extraction (_pragma/_metadata parsing)", "Adding new dependencies (L0 stays minimal)"]
safe = ["Documentation updates", "Adding format support (new extension mappings)", "Improving error messages", "Adding cache strategies", "New utility functions in exists/must/jsonc"]

# X.2 EXTENSION -- Future Ideas [DEFINED]
[package.metadata.omni._closing.X2_extension]
planned = ["YAML loading support", "Schema validation helpers", "Watch/hot-reload for development", "Async loading variants"]

# X.3 TROUBLESHOOTING [DEFINED]
[package.metadata.omni._closing.X3_troubleshooting]
scripture = "James 1:5 (WEB) -- If any lacks wisdom, let him ask of God."
workspace_not_found = "Ensure b-word/Cargo.toml exists with [workspace] section and this crate in members"
path_mismatch = "Workspace member path must match: L0-universal/c-hybrid/spec/config"
root_not_set = "Call config::set_root() before any load operations — sets the b-word base directory"
index_not_found = "Place index.toml at {root}/word/core/specs/index.toml — manifest-driven flow needs this"
identity_missing = "Loaded TOML files need [_pragma] or [_metadata] tables for identity extraction"

# X.4 REFERENCE [DEFINED]
[package.metadata.omni._closing.X4_reference]
related_files = [
    "src/lib.rs -- Public API surface (4-block Rust source)",
    "src/loader.rs -- Core loading: manifest, file, directory, batch flows",
    "src/cache.rs -- Dual-layer config cache with RwLock + poison recovery",
    "src/discovery.rs -- Manifest vs filesystem tripwire comparison",
    "src/graph.rs -- Dependency DAG construction + cycle detection",
    "src/identity.rs -- OmniCode identity module (PRAGMA + METADATA)",
    "src/types.rs -- Data types: IndexManifest, ConfigFile, LoadResult",
    "src/error.rs -- ConfigError enum with 4 variants",
    "src/format_loader.rs -- TOML/JSON/JSONC loading + config search",
    "src/formats.rs -- Extension-to-format mapping registry",
    "src/root.rs -- Filesystem root resolution",
    "src/exists.rs -- File/directory existence checks",
    "src/must.rs -- Panic-on-missing convenience wrappers",
    "src/jsonc.rs -- JSONC parsing (strip comments from JSON)",
    "go/loader.go -- Go counterpart (monolithic, b-01.00)",
    "tests/l0_integration.rs -- Cross-crate chain verification",
    "b-word/Cargo.toml -- Workspace root",
    "seed/code/L0/toml/cargo/Cargo.toml -- Template this derives from",
]
required_sections = ["package", "dependencies"]
validate = "cargo check -p bereshit-l0-config && cargo clippy -p bereshit-l0-config && cargo test -p bereshit-l0-config"

# X.5 NOTE -- Final Anchor [REQUIRED]
[package.metadata.omni._closing.X5_note]
note = "Configuration is vision made plain. The manifest declares what exists, the loader makes it accessible, the cache makes it fast, the tripwire keeps it honest. 14 modules, two loading flows, one purpose."
scripture = "Habakkuk 2:2-3 (KJV) -- Write the vision, and make it plain upon tables, that he may run that readeth it. For the vision is yet for an appointed time."
anchor = "L0 universal config. No OmniCode deps, no L1+. Every loaded file becomes self-aware through identity extraction."

# ===================================================================
# END CLOSING [END]
# ===================================================================

# #!omni code --toml --cargo

# ===================================================================
# Cargo.toml -- 3-Block (METADATA + CONTENT + CLOSING)
# ===================================================================
#
# Structure: 3-block (METADATA + CONTENT + CLOSING)
#
# TOML constraint: [package] must come before [package.metadata.*]
# sub-tables. So the physical order is:
#
#   [package]                        <- Cc.1 identity (CONTENT)
#   [package.metadata.omni.*]        <- I/C metadata (METADATA)
#   [package.metadata.omni._closing] <- X closing (CLOSING)
#   [dependencies] etc.              <- Co sections (CONTENT continued)
#
# Logical structure is still: METADATA -> CONTENT -> CLOSING
# Physical ordering respects TOML's sub-table rule.

# -------------------------------------------------------------------
# [package] -- MUST come first (TOML sub-table ordering rule)
# -------------------------------------------------------------------
#
# Cc.1 PACKAGE -- Crate Identity [REQUIRED]
#
# Typestate CLI scaffolding for all CPI-SI skill commands.
# Pure std -- no external dependencies beyond identity registration.
# Enforces parse-before-query at compile time via App -> ParsedApp.
[package]
name = "bereshit-l0-cli"
version = "0.3.0"
description = "Typestate CLI scaffolding for CPI-SI skill commands -- parse-before-query enforced at compile time"
edition = { workspace = true }
rust-version = { workspace = true }
authors = { workspace = true }
repository = { workspace = true }

# ===================================================================
# METADATA BLOCK [METADATA]
# ===================================================================
#
# OmniCode metadata lives in [package.metadata.omni] -- Cargo's
# sanctioned extension point. Cargo ignores everything under
# package.metadata.

# -------------------------------------------------------------------
# I -- Identity: What IS This File
# -------------------------------------------------------------------

[package.metadata.omni.I1_core]
key = "B-L0-hybrid-cli-cargo"
format = "@omni code --toml --cargo"
from = "b-word/seed/code/L0/toml/cargo/Cargo.toml"
at = "b-03.00"

[package.metadata.omni.I2_family]
type = "code"
subtype = "crate-manifest"
role = "derived"
structure = "3-block"

[package.metadata.omni.I3_instance]
file = "Cargo.toml"
title = "CLI Scaffolding Crate Manifest"
component = "Build manifest for typestate CLI scaffolding -- pure std + identity"
path = "L0-universal/c-hybrid/interface/cli/Cargo.toml"
provides = ["CLI_SCAFFOLDING_CRATE_MANIFEST"]
brief = "Pure std crate manifest. Workspace inheritance for shared settings. Only dependency: identity registration."

[package.metadata.omni.I4_architecture]
layers = "L0:C, L1:C, L2:C, L3:Go, L4:Go/Rust, L5:C#"
anchor = "genesis_1_1 -> void(0) -> unity(1) -> derivation"

# -------------------------------------------------------------------
# C -- Context: What Do I Need to KNOW
# -------------------------------------------------------------------

[package.metadata.omni.C1_state]
version = "b-03.00"
status = "Active"
created = "2025-12-10"
updated = "2026-02-18"

[package.metadata.omni.C2_attribution]
architect = "Seanje Lenox-Wise"
implementation = "Nova Dawn (CPI-SI)"
organization = "CreativeWorkzStudio LLC"
copyright = "2025-2026 CreativeWorkzStudio LLC. All rights reserved."

# "The heart of the righteous studieth to answer."
[package.metadata.omni.C3_grounding]
scripture = "Proverbs 15:28 (KJV) -- The heart of the righteous studieth to answer."
principle = "Study before answering. Parse input fully before acting on it."
anchor = "Genesis 1:1 -- In the beginning God created the heaven and the earth."

[package.metadata.omni.C4_dependencies]
requires = "Rust toolchain (rustc, cargo), workspace Cargo.toml for shared settings, bereshit-l0-identity for registration"
consumers = "All CPI-SI skill CLI commands"
integration = "Part of Cargo workspace. Workspace settings inherited via .workspace = true."
if_missing = "Crate cannot be built -- cargo has no manifest. All skill CLIs lose standard conventions."

[package.metadata.omni.C4_dependencies.related]
parent = "seed/code/L0/toml/cargo/Cargo.toml -- Cargo.toml template"
children = []

[package.metadata.omni.C4_dependencies.cross_format]
role = "Cargo.toml is TOML-native -- Cargo's manifest format."
note = "Pairs with src/lib.rs (Rust source) and go/cli.go (Go counterpart). Typestate pattern is Rust-only."

[package.metadata.omni.C5_intent]
purpose = "Build manifest for typestate CLI scaffolding -- pure std, zero external deps"
core_design = "Workspace inheritance -> identity dep only -> library target"
philosophy = "Convention over configuration. Consistent flags across all CPI-SI tools."

[package.metadata.omni.C5_intent.key_features]
identity = "Crate name, version, edition from workspace"
pure_std = "std only -- env, fmt, process. No external dependencies."
identity_dep = "bereshit-l0-identity for global component registry"
library = "Library target only -- no binary"
lints = "Workspace-level lint rules for consistency"

[package.metadata.omni.C6_roadmap]
history = ["0.1.0 (2025-12-10) -- Initial creation (Go)", "0.2.0 (2026-02-14) -- Rust rewrite with typestate pattern", "0.3.0 (2026-02-18) -- I/C metadata, version flag, standard flags, Display for Usage"]
current = "b-03.00 -- Typestate CLI with version support, standard flags, comprehensive tests."
planned = ["Subcommand support", "Custom flag types (string, integer)", "Shell completion generation"]

[package.metadata.omni.C7_classification]
tags = ["cli", "flags", "usage", "typestate", "pure", "no-deps", "interface", "omnicode", "cpi-si"]
domain = "L0-universal/interface/cli"

[package.metadata.omni._contract]
required_identity = ["I1_core", "I2_family", "I3_instance"]
required_context = ["C1_state", "C2_attribution", "C3_grounding", "C4_dependencies"]
extension_identity = ["I4_architecture"]
extension_context = ["C5_intent", "C6_roadmap", "C7_classification"]
required_closing = ["X1_policy", "X5_note"]
defined_closing = ["X2_extension", "X3_troubleshooting", "X4_reference"]
valid_patterns = ["I{N}_{name}", "C{N}_{name}", "X{N}_{name}", "_{name}"]
rule = "Metadata in package.metadata.omni. Standard Cargo sections for content."

# ===================================================================
# END METADATA [END]
# ===================================================================


# ===================================================================
# CONTENT BLOCK [CONTENT] (continued)
# ===================================================================
#
# [package] is above (TOML ordering constraint). Remaining
# content sections follow here.
#
# Zones:
#   Cc -- Configurations  Crate identity [package] and targets [lib]/[[bin]]
#   Co -- Operations      Dependencies, features, lints
#   Cv -- Validation      Build verification (comments only)
#
# Ordering: cargo_convention (package -> targets -> deps -> features -> lints)

# -------------------------------------------------------------------
# Cc.2 TARGETS -- Library Configuration [DEFAULT]
# -------------------------------------------------------------------
#
# Library crate -- default target. No [lib] section needed because
# Cargo infers lib from src/lib.rs. No [[bin]] because this is a
# library, not an executable.

# -------------------------------------------------------------------
# Co -- Content Operations
# -------------------------------------------------------------------

# Co.1 DEPENDENCIES -- Crate Dependencies [MINIMAL]
#
# Pure std + identity only. No external dependencies.
# The identity crate enables register_identity() for the global
# component registry. Everything else is std::{env, fmt, process}.
[dependencies]
bereshit-l0-identity = { workspace = true }

[dev-dependencies]

# Co.2 FEATURES -- Optional Capabilities [DEFINED]
#
# Future: subcommand support, custom flag types, shell completion.
# Currently empty because boolean flags cover all current use cases.
[features]
default = []

# Co.3 LINTS -- Code Quality Rules [WORKSPACE]
#
# Workspace-level lints keep all crates consistent.
[lints]
workspace = true

# -------------------------------------------------------------------
# Cv -- Content Validation (comments only)
# -------------------------------------------------------------------
#
#   cargo check  -p bereshit-l0-cli    -- Type-check
#   cargo build  -p bereshit-l0-cli    -- Full build
#   cargo test   -p bereshit-l0-cli    -- Run tests (41 unit + 10 doc)
#   cargo clippy -p bereshit-l0-cli    -- Lint check
#   cargo fmt --check                  -- Format check

# ===================================================================
# END CONTENT [END]
# ===================================================================


# ===================================================================
# CLOSING BLOCK [CLOSING]
# ===================================================================

# X.1 POLICY -- Modification Guidelines [REQUIRED]
[package.metadata.omni._closing.X1_policy]
scripture = "Proverbs 22:28 (WEB) -- Don't move the ancient boundary stone."
never = ["Add heavy dependencies (networking, database, filesystem)", "Remove workspace inheritance", "Add binary targets to this library crate", "Remove bereshit-l0-identity dependency (breaks registration)"]
careful = ["Version bumps (public API contract)", "Type signature changes (all skill CLIs affected)", "New dependencies (this crate should stay near-zero-dep)"]
safe = ["Documentation updates", "Dev-dependency additions for testing", "Feature flag definitions for future capabilities"]

# X.2 EXTENSION -- Future Ideas [DEFINED]
[package.metadata.omni._closing.X2_extension]
planned = ["Subcommand support (App::subcommand)", "Custom flag types (string, integer)", "Shell completion generation (bash, zsh, fish)", "C ABI bridge for libtrit integration"]

# X.3 TROUBLESHOOTING [DEFINED]
[package.metadata.omni._closing.X3_troubleshooting]
scripture = "James 1:5 (WEB) -- If any lacks wisdom, let him ask of God."
workspace_not_found = "Ensure b-word/Cargo.toml exists with [workspace] section and this crate in members"
path_mismatch = "Workspace member path must match: L0-universal/c-hybrid/interface/cli"
identity_not_found = "bereshit-l0-identity must be in workspace.dependencies"
doc_tests_fail = "Doc tests use crate name bereshit_l0_cli. Ensure Cargo.toml name matches."

# X.4 REFERENCE [DEFINED]
[package.metadata.omni._closing.X4_reference]
related_files = [
    "src/lib.rs -- Crate root, typestate CLI scaffolding",
    "go/cli.go -- Go counterpart (convention-based, no typestate)",
    "seed/code/L0/toml/cargo/Cargo.toml -- Template this derives from",
    "core/foundation/Cargo.toml -- Foundation crate manifest (reference pattern)",
]
required_sections = ["package"]
validate = "cargo check -p bereshit-l0-cli && cargo clippy -p bereshit-l0-cli && cargo test -p bereshit-l0-cli"

# X.5 NOTE -- Final Anchor [REQUIRED]
[package.metadata.omni._closing.X5_note]
note = "Typestate CLI scaffolding. Pure std + identity. Parse-before-query enforced at compile time."
scripture = "Proverbs 15:28 (KJV) -- The heart of the righteous studieth to answer."
anchor = "Study before answering. The typestate pattern IS this principle in code."

# ===================================================================
# END CLOSING [END]
# ===================================================================

# #!omni document --toml

# ═══════════════════════════════════════════════════════════════
# METADATA BLOCK [METADATA]
# ═══════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────
# I — Identity: What IS This File
# ─────────────────────────────────────────────────────────────

[_metadata.I1_core]
key = "B-L0-types-metadata-slots"
format = "@omni document --toml"
from = "seed/data/L0/toml/config.toml"
at = "document"

[_metadata.I2_family]
type = "data"
subtype = "types"
role = "rung"
structure = "3-block"

[_metadata.I3_instance]
file = "metadata-slots.toml"
title = "OmniCode Metadata Slot Types"
component = "Metadata slot definitions — 13 slots completing BRIDGE equation. 4 categories: Reserved, Config, Resource, System."
path = "metadata-slots.toml"
provides = ["METADATA_SLOT_DEFINITIONS", "BRIDGE_EQUATION", "SLOT_CATEGORIES"]
brief = "13 metadata slots completing BRIDGE equation — 243 (PENTERACT) + 13 (BRIDGE) = 256. Slot 0 reserved (anchor), slots 1-12 across 3 categories."

[_metadata.I4_architecture]
layers = "L0:C, L3:Go, L5:C#"
anchor = "genesis_1_1 -> void(0) -> unity(1) -> derivation"

# ─────────────────────────────────────────────────────────────
# C — Context: What Do I Need to KNOW
# ─────────────────────────────────────────────────────────────

[_metadata.C1_state]
version = "a-01.00"
status = "Active"
created = "2026-01-25"
updated = "2026-02-16"

[_metadata.C2_attribution]
architect = "Seanje Lenox-Wise"
implementation = "Nova Dawn (CPI-SI)"
organization = "CreativeWorkzStudio LLC"
copyright = "© 2025-2026 CreativeWorkzStudio LLC. All rights reserved."

[_metadata.C3_grounding]
scripture = "Exodus 25:21 — And thou shalt put the mercy seat above upon the ark; and in the ark thou shalt put the testimony that I shall give thee."
principle = "The 13 metadata slots complete the BRIDGE equation (243 + 13 = 256). Like the mercy seat covering the ark, metadata covers the data. Slot 0 is reserved (the anchor), slots 1-12 are working slots."
anchor = "Exodus 25:22 — And there I will meet with thee. Metadata is where we meet the data — the interface layer."

[_metadata.C4_dependencies]
requires = ["primitives/primitives-catalog.toml", "math/core/definitions.toml"]
consumers = ["Error encoding (metadata slots)", "Config system (slots 1-4)", "Resource tracking (slots 5-8)", "System errors (slots 9-12)"]
integration = "Loaded by Go config loader, generates C header via index.toml"
if_missing = "Error encoding falls back to raw byte values — no categorical metadata"

[_metadata.C4_dependencies.cross_format]
role = "TOML defines slot structure and the BRIDGE equation. C header generated from these definitions."
note = "No JSON/JSONC counterpart — BRIDGE equation is mathematical specification."

[_metadata.C5_intent]
purpose = "Define the 13 metadata slots in the BRIDGE equation"
core_design = "THE BRIDGE EQUATION: 243 (PENTERACT) + 13 (BRIDGE) = 256"
philosophy = "243 + 13 = 256. The BRIDGE completes the byte. Metadata bridges raw data to meaningful interpretation."

[_metadata.C6_roadmap]
history = [
    "a-01.00 (2026-01-25) — Created as standalone metadata slot specification",
    "a-01.00 (2026-02-10) — Moved to new structure, TOML-native 3-block format",
    "a-01.00 (2026-02-16) — I/C metadata upgrade",
]
current = "13 slots with categories, ordinals, and bounds"
planned = [
    "Slot allocation protocol",
    "Dynamic slot negotiation",
]

[_metadata.C7_classification]
tags = ["metadata", "slots", "bridge", "243+13=256", "penteract", "L0", "types"]

# ─────────────────────────────────────────────────────────────
# Contract
# ─────────────────────────────────────────────────────────────

[_metadata._contract]
required_identity = ["I1_core", "I2_family", "I3_instance"]
required_context = ["C1_state", "C2_attribution", "C3_grounding", "C4_dependencies"]
extension_identity = ["I4_architecture"]
extension_context = ["C5_intent", "C6_roadmap", "C7_classification"]
required_closing = ["X1_policy", "X5_note"]
defined_closing = ["X2_extension", "X3_troubleshooting", "X4_reference"]

# ═══════════════════════════════════════════════════════════════
# END METADATA [END]
# ═══════════════════════════════════════════════════════════════


# ═══════════════════════════════════════════════════════════════
# CONTENT BLOCK [CONTENT]
# ═══════════════════════════════════════════════════════════════

[_content]
format = "TOML — native tables, UTF-8, LF line endings"
key_convention = "UPPER_SNAKE for slot names, lower_snake for properties"
constraint = "13 slots exactly (0-12). Ordinals = 243 + slot. Categories: Reserved(0), Config(1-4), Resource(5-8), System(9-12)."
Cc = "Content Configurations — 13 metadata slots (Reserved, Config, Resource, System categories)"
Co = "Content Operations — slot assignment, ordinal calculation (243 + slot), bounds"
Cv = "Content Validation — schema requirements, health scoring"

# ─────────────────────────────────────────────────────────────
# Identity
# ─────────────────────────────────────────────────────────────

[identity]
name = "OmniCode Metadata Slot Types"
version = "a-01.00"
domain = "types"
scope = "13 metadata slots completing BRIDGE equation — 243 + 13 = 256"

# ─────────────────────────────────────────────────────────────
# Cc — Content Configurations [CONFIG]
# ─────────────────────────────────────────────────────────────

[metadata]
description = "13 metadata slots completing BRIDGE equation"
count = 13
reserved = 1
working = 12

# The BRIDGE equation
[metadata.bridge]
penteract = 243
bridge = 13
total = 256
description = "243 + 13 = 256 — BRIDGE completes the byte"

# Categories (4 slots each after reserved)
[metadata.categories]
description = "4 categories organizing the 12 working slots"

RESERVED = { slots = [0], description = "Anchor slot (fallback)" }
CONFIG   = { slots = [1, 2, 3, 4], description = "Configuration metadata" }
RESOURCE = { slots = [5, 6, 7, 8], description = "Resource metadata" }
SYSTEM   = { slots = [9, 10, 11, 12], description = "System metadata" }

# ─────────────────────────────────────────────────────────────
# Co — Content Operations [OPS]
# ─────────────────────────────────────────────────────────────

# Individual slot definitions
[metadata.slots]
description = "13 slots with ordinal values (243 + slot)"

RESERVED   = { slot = 0,  ordinal = 243, description = "Fallback/anchor" }
VERSION    = { slot = 1,  ordinal = 244, category = "CONFIG", description = "Version mismatch" }
CONFIG     = { slot = 2,  ordinal = 245, category = "CONFIG", description = "Configuration error" }
STATE      = { slot = 3,  ordinal = 246, category = "CONFIG", description = "Invalid state" }
INIT       = { slot = 4,  ordinal = 247, category = "CONFIG", description = "Initialization failed" }
MEMORY     = { slot = 5,  ordinal = 248, category = "RESOURCE", description = "Memory allocation" }
TIMEOUT    = { slot = 6,  ordinal = 249, category = "RESOURCE", description = "Operation timeout" }
PERMISSION = { slot = 7,  ordinal = 250, category = "RESOURCE", description = "Permission denied" }
RESOURCE   = { slot = 8,  ordinal = 251, category = "RESOURCE", description = "Resource unavailable" }
ASSERT     = { slot = 9,  ordinal = 252, category = "SYSTEM", description = "Assertion failed" }
OVERFLOW   = { slot = 10, ordinal = 253, category = "SYSTEM", description = "Numeric overflow" }
UNDERFLOW  = { slot = 11, ordinal = 254, category = "SYSTEM", description = "Numeric underflow" }
FORMAT     = { slot = 12, ordinal = 255, category = "SYSTEM", description = "Format error (MAX)" }

# Bounds
[metadata.bounds]
slot_min = 0
slot_max = 12
ordinal_min = 243
ordinal_max = 255

# ─────────────────────────────────────────────────────────────
# Cv — Content Validation [VALIDATE]
# ─────────────────────────────────────────────────────────────

[_validation]
commands = ["cws-struct lint toml metadata-slots.toml"]

[_validation.schema]
required = ["identity", "metadata", "metadata.bridge", "metadata.categories", "metadata.slots", "metadata.bounds"]
optional = ["extensions"]

[_validation.health_scoring]
total = 100
correct_structure = 40
valid_values = 30
bridge_integrity = 30

# ═══════════════════════════════════════════════════════════════
# END CONTENT [END]
# ═══════════════════════════════════════════════════════════════


# ═══════════════════════════════════════════════════════════════
# CLOSING BLOCK [CLOSING]
# ═══════════════════════════════════════════════════════════════

[_closing.X1_policy]
scripture = "Proverbs 22:28 (KJV) — Remove not the ancient landmark, which thy fathers have set."
never = [
    "Remove slot definitions (breaks error encoding)",
    "Change ordinal values (breaks binary protocol)",
    "Remove BRIDGE equation constants",
]
careful = [
    "Redefining slot categories",
    "Changing slot descriptions",
    "Adding new slots (impossible — 13 is fixed)",
]
safe = [
    "Updating descriptions",
    "Adding metadata",
    "Adding usage examples",
]

[_closing.X2_extension]
planned = [
    "Slot allocation protocol",
    "Dynamic slot negotiation",
]

[_closing.X3_troubleshooting]
ordinal_out_of_range = "Ordinals must be 243-255. Verify: ordinal = 243 + slot."
category_mismatch = "Check slot index against category ranges: Reserved(0), Config(1-4), Resource(5-8), System(9-12)."

[_closing.X4_reference]
related_files = [
    "math/core/definitions.toml — PENTERACT (243), ternary math",
    "primitives/primitives-catalog.toml — Base types (uint8_t)",
    "errors/foundation.toml — Error system using metadata slots",
]

[_closing.X5_note]
note = "THE BRIDGE EQUATION: 243 + 13 = 256. The BRIDGE completes the byte. Metadata bridges raw data to meaningful interpretation."
scripture = "John 1:14 — And the Word was made flesh, and dwelt among us."
anchor = "In the beginning God created — Genesis 1:1. The BRIDGE connects ternary to binary, just as the Word connects heaven to earth."

# ═══════════════════════════════════════════════════════════════
# END CLOSING [END]
# ═══════════════════════════════════════════════════════════════

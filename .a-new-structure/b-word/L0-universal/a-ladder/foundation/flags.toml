# #!omni document --toml

# ═══════════════════════════════════════════════════════════════
# METADATA BLOCK [METADATA]
# ═══════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────
# I — Identity: What IS This File
# ─────────────────────────────────────────────────────────────

[_metadata.I1_core]
key = "B-L0-types-flags"
format = "@omni document --toml"
from = "seed/data/L0/toml/config.toml"
at = "document"

[_metadata.I2_family]
type = "data"
subtype = "types"
role = "rung"
structure = "3-block"

[_metadata.I3_instance]
file = "flags.toml"
title = "Restore Strategy Flags"
component = "Restore strategy bit flags — 6 flags (bits 0-5) defining recovery actions for the DAR restore phase. Composable via bitmask."
path = "flags.toml"
provides = ["RESTORE_FLAG_DEFINITIONS", "RESTORE_COMPOSITES", "FLAG_OPERATIONS"]
brief = "Restore strategy bit flags. 6 flags (bits 0-5), bitmask operations, composable strategies. BASIC = FILL + CLAMP. FULL = all 6 flags. Scoped to restore/recovery — other flag types get their own files."

[_metadata.I4_architecture]
layers = "L0:C, L3:Go, L5:C#"
anchor = "genesis_1_1 -> void(0) -> unity(1) -> derivation"

# ─────────────────────────────────────────────────────────────
# C — Context: What Do I Need to KNOW
# ─────────────────────────────────────────────────────────────

[_metadata.C1_state]
version = "a-01.00"
status = "Active"
created = "2026-01-25"
updated = "2026-02-16"

[_metadata.C2_attribution]
architect = "Seanje Lenox-Wise"
implementation = "Nova Dawn (CPI-SI)"
organization = "CreativeWorkzStudio LLC"
copyright = "© 2025-2026 CreativeWorkzStudio LLC. All rights reserved."

[_metadata.C3_grounding]
scripture = "Exodus 19:12 — Thou shalt set bounds unto the people round about"
principle = "Flags set bounds — each bit is a boundary marker that defines what actions are permitted. Just as God set bounds on Sinai, flags set bounds on restore strategies."
anchor = "Job 38:11 — Thus far shalt thou come, but no further. Each flag limits or enables specific recovery actions."

[_metadata.C4_dependencies]
requires = ["primitives/primitives-catalog.toml"]
consumers = ["Restore system (strategy flags)", "Health recovery (flag composition)", "DAR restore phase (flag testing)"]
integration = "Loaded by Go config loader, generates C header via index.toml"
if_missing = "Restore system falls back to NONE strategy — no recovery actions"

[_metadata.C4_dependencies.cross_format]
role = "TOML defines flag structure and operations. C header generated from these definitions."
note = "No JSON/JSONC counterpart — flags are type specification, not runtime data."

[_metadata.C5_intent]
purpose = "Define restore strategy flags for DAR recover phase — scoped to recovery actions only"
core_design = "6 flags (bits 0-5), bitmask operations, composable strategies"
philosophy = "Flags compose to form strategies. BASIC = FILL + CLAMP. FULL = all 6 flags. Each flag is a specific recovery action."

[_metadata.C6_roadmap]
history = [
    "a-01.00 (2026-01-25) — Created as standalone flag type specification",
    "a-01.00 (2026-02-10) — Moved to new structure, TOML-native 3-block format",
    "a-01.00 (2026-02-16) — I/C metadata upgrade",
]
current = "6 restore strategy flags with composite strategies and operations"
planned = [
    "Flag validation macros",
    "Strategy composition helpers",
]

[_metadata.C7_classification]
tags = ["flags", "bitmask", "restore", "strategy", "L0", "types"]

# ─────────────────────────────────────────────────────────────
# Contract
# ─────────────────────────────────────────────────────────────

[_metadata._contract]
required_identity = ["I1_core", "I2_family", "I3_instance"]
required_context = ["C1_state", "C2_attribution", "C3_grounding", "C4_dependencies"]
extension_identity = ["I4_architecture"]
extension_context = ["C5_intent", "C6_roadmap", "C7_classification"]
required_closing = ["X1_policy", "X5_note"]
defined_closing = ["X2_extension", "X3_troubleshooting", "X4_reference"]

# ═══════════════════════════════════════════════════════════════
# END METADATA [END]
# ═══════════════════════════════════════════════════════════════


# ═══════════════════════════════════════════════════════════════
# CONTENT BLOCK [CONTENT]
# ═══════════════════════════════════════════════════════════════

[_content]
format = "TOML — native tables, UTF-8, LF line endings"
key_convention = "UPPER_SNAKE for flag names, lower_snake for properties"
constraint = "All flags within 6-bit mask (0x3F). Composite strategies combine individual flags."
Cc = "Content Configurations — individual flags (6-bit), composite strategies, flag combinations"
Co = "Content Operations — flag combination rules, mask application, flag testing"
Cv = "Content Validation — schema requirements, health scoring"

# ─────────────────────────────────────────────────────────────
# Identity
# ─────────────────────────────────────────────────────────────

[identity]
name = "Restore Strategy Flags"
version = "a-01.00"
domain = "types"
scope = "Restore strategy bit flags — 6 flags (bits 0-5), bitmask operations, composable strategies"

# ─────────────────────────────────────────────────────────────
# Cc — Content Configurations [CONFIG]
# ─────────────────────────────────────────────────────────────

[flags]
description = "Restore strategy bit flags"
count = 6
max_bit = 5
mask = "0x3F"

# Individual flags
[flags.restore_strategy]
description = "6 restore strategy flags (bits 0-5)"

NONE           = { bit = -1, value = "0x00", description = "No recovery" }
FILL_DEFAULTS  = { bit = 0,  value = "0x01", description = "Fill missing with defaults" }
LOCATE_FILES   = { bit = 1,  value = "0x02", description = "Search for misplaced files" }
CREATE_DIRS    = { bit = 2,  value = "0x04", description = "Create missing directories" }
CLAMP_VALUES   = { bit = 3,  value = "0x08", description = "Clamp out-of-range values" }
FALLBACK       = { bit = 4,  value = "0x10", description = "Use fallback resources" }
DEGRADE        = { bit = 5,  value = "0x20", description = "Graceful degradation" }

# ─────────────────────────────────────────────────────────────
# Co — Content Operations [OPS]
# ─────────────────────────────────────────────────────────────

# Composite strategies
[flags.composites]
BASIC = { flags = ["FILL_DEFAULTS", "CLAMP_VALUES"], description = "Minimal safe recovery" }
FULL  = { flags = ["FILL_DEFAULTS", "LOCATE_FILES", "CREATE_DIRS", "CLAMP_VALUES", "FALLBACK", "DEGRADE"], description = "Maximum recovery effort" }

# Operations
[flags.operations]
HAS_FLAG   = { signature = "(flags, flag) -> bool", description = "Test if flag is set" }
SET_FLAG   = { signature = "(flags, flag) -> flags", description = "Set a flag" }
CLEAR_FLAG = { signature = "(flags, flag) -> flags", description = "Clear a flag" }
IS_VALID   = { signature = "(flags) -> bool", description = "Check flags are within mask" }

# ─────────────────────────────────────────────────────────────
# Cv — Content Validation [VALIDATE]
# ─────────────────────────────────────────────────────────────

[_validation]
commands = ["cws-struct lint toml flags.toml"]

[_validation.schema]
required = ["identity", "flags", "flags.restore_strategy", "flags.composites", "flags.operations"]
optional = ["extensions"]

[_validation.health_scoring]
total = 100
correct_structure = 40
valid_values = 30
flag_completeness = 30

# ═══════════════════════════════════════════════════════════════
# END CONTENT [END]
# ═══════════════════════════════════════════════════════════════


# ═══════════════════════════════════════════════════════════════
# CLOSING BLOCK [CLOSING]
# ═══════════════════════════════════════════════════════════════

[_closing.X1_policy]
scripture = "Proverbs 22:28 (KJV) — Remove not the ancient landmark, which thy fathers have set."
never = [
    "Remove flag definitions without updating consuming code",
    "Change bit positions (breaks binary compatibility)",
    "Remove operations (breaks API contract)",
]
careful = [
    "Adding new flags (must fit in mask)",
    "Changing composite strategies",
    "Adding new operations",
]
safe = [
    "Updating descriptions",
    "Adding new composite strategies",
    "Adding metadata",
]

[_closing.X2_extension]
planned = [
    "Flag validation macros",
    "Strategy composition helpers",
]

[_closing.X3_troubleshooting]
flag_not_recognized = "Verify bit position matches header definition. Check mask bounds."
strategy_incomplete = "Review composite strategy — all referenced flags must exist."

[_closing.X4_reference]
related_files = [
    "primitives/primitives-catalog.toml — Base types (uint8_t for bitmask storage)",
    "errors/foundation.toml — Error system using flags",
]

[_closing.X5_note]
note = "Flags set bounds — each bit is a boundary marker. Compose flags into strategies. BASIC for safe recovery, FULL for maximum effort."
scripture = "In the beginning God created the heaven and the earth. — Genesis 1:1"
anchor = "Boundaries before content. Flags define the boundaries of recovery."

# ═══════════════════════════════════════════════════════════════
# END CLOSING [END]
# ═══════════════════════════════════════════════════════════════

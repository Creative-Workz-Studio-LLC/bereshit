# #!omni data --toml

# ═══════════════════════════════════════════════════════════════
# METADATA BLOCK [METADATA]
# ═══════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────
# I — Identity: What IS This File
# ─────────────────────────────────────────────────────────────

[_metadata.I1_core]
key = "B-L0-bible-text-codec"
format = "@omni data --toml"
from = "seed/data/L0/toml/config.toml"
at = "data"

[_metadata.I2_family]
type = "data"
subtype = "bible"
role = "rung"
structure = "3-block"

[_metadata.I3_instance]
file = "text-codec.toml"
title = "Scripture Text Codec"
component = "Character encoding/decoding algorithms — text ↔ trit5 ↔ binary conversion"
path = ".staging/bible/text/text-codec.toml"
provides = ["TEXT_CODEC_ALGORITHMS"]
brief = "Algorithms for converting Scripture text to/from trit5 byte sequences and building/reading scripture.bin."

[_metadata.I4_architecture]
layers = "L0:C, L1:C, L2:C, L3:Go, L4:Go/Rust, L5:C#"
anchor = "genesis_1_1 -> void(0) -> unity(1) -> derivation"

# ─────────────────────────────────────────────────────────────
# C — Context: What Do I Need to KNOW
# ─────────────────────────────────────────────────────────────

[_metadata.C1_state]
version = "a-01.00"
status = "Active"
created = "2025-12-14"
updated = "2026-02-16"

[_metadata.C2_attribution]
architect = "Seanje Lenox-Wise"
implementation = "Nova Dawn (CPI-SI)"
organization = "CreativeWorkzStudio LLC"
copyright = "© 2025-2026 CreativeWorkzStudio LLC. All rights reserved."

[_metadata.C3_grounding]
scripture = "Psalm 12:6 (KJV) — The words of the LORD are pure words: as silver tried in a furnace of earth, purified seven times."
principle = "Encoding preserves purity — encode(decode(x)) == x for all valid inputs. No loss, no approximation."
anchor = "Genesis 1:1 anchors the codec. Pure words in, pure words out."

[_metadata.C4_dependencies]
requires = ["character-table.toml", "scripture-format.toml"]
consumers = ["scripture encoder binary", "scripture decoder binary", "verse lookup tools"]
integration = "Algorithms define the codec contract. Code generators produce C/Go/Rust implementations."
if_missing = "Cannot encode or decode Scripture text — algorithms undefined"

[_metadata.C4_dependencies.cross_format]
role = "Codec ALGORITHMS in TOML (pseudocode). No JSONC/JSON counterpart."
note = "TOML defines algorithms. Generated code implements them in C, Go, Rust."

[_metadata.C5_intent]
purpose = "Character encoding/decoding algorithms for Scripture text ↔ trit5 binary conversion"
core_design = "Four levels: character ↔ trit5 (single char), text ↔ bytes (string), verse encode/decode (with index entry), full file build (header + index + data)."
philosophy = "Reversibility is the contract. encode(decode(x)) == x. Every character preserved through the round trip."

[_metadata.C6_roadmap]
history = [
    "a-01.00 (2025-12-14) — Initial creation (part of scripture-text.toml)",
    "a-01.00 (2026-02-16) — Split from scripture-text.toml into dedicated codec file"
]
current = "Codec split — standalone algorithm specification with I/C metadata"
planned = ["Streaming encode/decode for large texts", "Compression-aware codec variants"]

[_metadata.C7_classification]
tags = ["bible", "codec", "encoding", "decoding", "trit5", "text", "L0", "foundation"]

[_metadata._contract]
required_identity = ["I1_core", "I2_family", "I3_instance"]
required_context = ["C1_state", "C2_attribution", "C3_grounding", "C4_dependencies"]
extension_identity = ["I4_architecture"]
extension_context = ["C5_intent", "C6_roadmap", "C7_classification"]
required_closing = ["X1_policy", "X5_note"]
defined_closing = ["X2_extension", "X3_troubleshooting", "X4_reference"]
valid_patterns = ["I{N}_{name}", "C{N}_{name}", "X{N}_{name}", "_{name}"]
rule = "Required sections are structural. Extensions follow naming patterns."

# ═══════════════════════════════════════════════════════════════
# END METADATA [END]
# ═══════════════════════════════════════════════════════════════


# ═══════════════════════════════════════════════════════════════
# CONTENT BLOCK [CONTENT]
# ═══════════════════════════════════════════════════════════════

[_content]
format = "TOML — UTF-8, LF line endings"
key_convention = "lowercase_with_underscores"
constraint = "Bible data file — text encoding/decoding algorithms for Scripture storage"
Cc = "Content Configurations — character codec, text codec, verse codec, file builder"
Co = "Content Operations — (algorithm spec: pseudocode definitions, no runtime ops here)"
Cv = "Content Validation — schema requirements, health scoring"

# ─────────────────────────────────────────────────────────────
# Cc — Content Configurations
# ─────────────────────────────────────────────────────────────

# Cc.0 IDENTITY — What Is This Config? [REQUIRED]
[identity]
name = "scripture-text-codec"
version = "a-01.00"
description = "Character encoding/decoding algorithms for Scripture text ↔ trit5 binary"
type = "bible-data"

# Cc.1 ENCODING ALGORITHMS — Text → Trit5 → Binary

[codec]
description = "Character encoding and decoding algorithms"
contract = "encode(decode(x)) == x for all valid inputs"

# Cc.1a CHARACTER LEVEL — Single character ↔ trit5 value

[codec.char_to_trit5]
description = "Convert single character to trit5 value"
algorithm = """
def char_to_trit5(char):
    code = ord(char)
    if code <= 127:
        # ASCII range: direct mapping
        return code
    elif code in extended_table:
        # Extended range: lookup mapping
        return extended_table[code]
    else:
        # Unknown character: use replacement
        return 63  # '?' character
"""

[codec.trit5_to_char]
description = "Convert trit5 value to character"
algorithm = """
def trit5_to_char(value):
    if value <= 127:
        # ASCII range: direct mapping
        return chr(value)
    elif value in reverse_extended_table:
        # Extended range: reverse lookup
        return reverse_extended_table[value]
    else:
        # Unknown value: use replacement
        return '?'
"""

# Cc.1b TEXT LEVEL — String ↔ byte sequence

[codec.text_to_bytes]
description = "Convert text string to trit5 byte sequence"
algorithm = """
def text_to_bytes(text):
    result = []
    for char in text:
        result.append(char_to_trit5(char))
    result.append(0)  # NUL terminator
    return bytes(result)
"""

[codec.bytes_to_text]
description = "Convert trit5 byte sequence to text string"
algorithm = """
def bytes_to_text(data):
    result = []
    for byte in data:
        if byte == 0:
            break  # NUL terminator
        result.append(trit5_to_char(byte))
    return ''.join(result)
"""

# Cc.1c VERSE LEVEL — Complete verse encode/decode with index entry

[codec.encode_verse]
description = "Encode complete verse for storage"
algorithm = """
def encode_verse(text, ordinal, version):
    # Convert text to trit5 bytes
    encoded = text_to_bytes(text)

    # Create index entry
    entry = IndexEntry(
        text_offset=current_data_offset,
        text_length=len(encoded),
        flags=0 if version == KJV else 1
    )

    return entry, encoded
"""

[codec.decode_verse]
description = "Decode verse from storage"
algorithm = """
def decode_verse(data, entry):
    # Extract verse bytes using index entry
    verse_bytes = data[entry.text_offset:entry.text_offset + entry.text_length]

    # Convert back to text
    return bytes_to_text(verse_bytes)
"""

# Cc.1d FILE LEVEL — Build complete scripture.bin

[codec.build_file]
description = "Build complete scripture.bin file"
algorithm = """
def build_scripture_file(kjv_verses, web_verses):
    # 1. Build header
    header = Header(
        magic=b'SCRP',
        version=1,
        kjv_count=len(kjv_verses),
        web_count=len(web_verses)
    )

    # 2. Build index and data sections
    index_entries = []
    text_data = bytearray()

    # KJV verses first
    for ordinal, text in kjv_verses:
        entry, encoded = encode_verse(text, ordinal, KJV)
        index_entries.append(entry)
        text_data.extend(encoded)

    # WEB verses second
    for ordinal, text in web_verses:
        entry, encoded = encode_verse(text, ordinal, WEB)
        index_entries.append(entry)
        text_data.extend(encoded)

    # 3. Calculate offsets
    header.index_offset = 64  # After header
    header.data_offset = 64 + (len(index_entries) * 8)

    # 4. Write file
    write_header(header)
    write_index(index_entries)
    write_data(text_data)
"""

# Cc.2 CODEC PROPERTIES

[codec.properties]
reversibility = "encode(decode(x)) == x for all valid inputs"
efficiency = "ASCII-compatible — most Scripture text is 1 byte per character"
extensibility = "115 spare values available for future character additions"
fallback = "Unknown characters map to '?' (trit5 value 63) — never crashes, always produces output"

# ─────────────────────────────────────────────────────────────
# Co — Content Operations
# ─────────────────────────────────────────────────────────────

# (Algorithm spec: pseudocode definitions. Runtime implementations generated from this.)

# ─────────────────────────────────────────────────────────────
# Cv — Content Validation
# ─────────────────────────────────────────────────────────────

[_validation]
commands = ["cws-struct lint toml text-codec.toml"]

[_validation.schema]
required = ["codec"]
optional = []

[_validation.health_scoring]
total = 100
correct_structure = 30
algorithm_completeness = 40
reversibility_documented = 30

# ═══════════════════════════════════════════════════════════════
# END CONTENT [END]
# ═══════════════════════════════════════════════════════════════


# ═══════════════════════════════════════════════════════════════
# CLOSING BLOCK [CLOSING]
# ═══════════════════════════════════════════════════════════════

[_closing.X1_policy]
scripture = "Proverbs 22:28 (KJV) — Remove not the ancient landmark, which thy fathers have set."
never = [
    "Reversibility contract — encode(decode(x)) == x is non-negotiable",
    "NUL terminator convention — required for verse boundaries",
    "ASCII direct mapping (0-127) — compatibility requirement",
]
careful = [
    "Changing fallback character — affects unknown character handling",
    "Modifying algorithm signatures — affects all implementations",
]
safe = [
    "Optimization notes",
    "Additional algorithm variants (streaming, compressed)",
    "Implementation guidance for specific languages",
]

[_closing.X2_extension]
planned = ["Streaming codec for large texts", "Compression-aware variants"]

[_closing.X3_troubleshooting]
round_trip_failure = "Check character table completeness — every trit5 value must have a reverse mapping"
nul_in_text = "NUL (0) is reserved as terminator — text containing NUL bytes will be truncated"
unknown_character = "Falls back to '?' (63) — check extended_table if specific characters are missing"

[_closing.X4_reference]
related_files = [
    ".staging/bible/text/character-table.toml — Character-to-trit5 value mapping (data)",
    ".staging/bible/text/scripture-format.toml — Binary file format spec (structure)",
    ".staging/bible/addressing/ — Verse addressing for ordinal lookups",
]

[_closing.X5_note]
note = "Pure words in, pure words out. The codec preserves every letter through the round trip."
scripture = "Psalm 12:6 (KJV) — The words of the LORD are pure words: as silver tried in a furnace of earth, purified seven times."
anchor = "Reversibility is the covenant. What goes in comes back unchanged. Genesis 1:1 anchors the codec."

# ═══════════════════════════════════════════════════════════════
# END CLOSING [END]
# ═══════════════════════════════════════════════════════════════

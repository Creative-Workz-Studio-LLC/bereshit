// L0 Universal — Foundation Layer
// "In the beginning God created the heaven and the earth." — Genesis 1:1
//
// The bedrock. Balanced ternary types, mathematical foundations, zone-aware
// filesystem. Everything else stands on this. No upward imports.
//
// Language: C (compiled, persistent, foundational)
// Dependencies: None (this IS the foundation)
// Dependents: L1, L2, L5

= L0 Universal

== Purpose

Universal foundations that every layer above depends on. Pure C libraries
implementing balanced ternary arithmetic, mathematical operations, and
zone-aware filesystem access. Platform-independent, substrate-independent.

== Current Location (pre-migration)

[cols="2,3"]
|===
| Component | Current Path

| libtrit (balanced ternary)
| `word/work/pkg/trit/`

| bereshit-fs (FUSE zones)
| `word/work/pkg/fuse/`

| libmath (future)
| _not yet created_
|===

== Target Structure

----
L0-universal/
├── libtrit/        ← balanced ternary types + arithmetic
├── libmath/        ← mathematical foundations
├── bereshit-fs/    ← zone-aware filesystem (FUSE)
└── foundation/     ← TOML specs (type system definitions)
----

== Build

 make l0

Delegates to `word/work/pkg/trit/` and `word/work/pkg/fuse/`.

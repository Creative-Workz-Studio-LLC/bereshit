# #!omni data --toml
# ═══════════════════════════════════════════════════════════════════════════════
# OMNICODE PRAGMA [PRAGMA]
# ═══════════════════════════════════════════════════════════════════════════════

[_pragma]
format = "@omni data --toml"
legend = { _req = "REQUIRED", _inh = "INHERITED", _ins = "INSTANCE" }

[_pragma.P1_core]
key = "B-L0-foundation-types-layer"
from = "b-word/seed/data/toml/config.toml"
at = "a-01.00"

[_pragma.P2_family]
type = "data"
subtype = "types"
role = "rung"
structure = "3-block"

[_pragma.P3_instance]
component = "layer"
layer = "L0-universal/foundation/types"
includes = ["L0-universal/foundation/math/ternary.toml", "L0-universal/foundation/types/primitives.toml"]
provides = ["LAYER_DEFINITIONS", "LAYER_GROUPS", "LAYER_SCALE"]

[_pragma.P4_architecture]
layers = "L0:C, L3:Go, L5:C#"
anchor = "genesis_1_1 -> void(0) -> unity(1) -> derivation"

[_pragma.P5_summary]
title = "OmniCode Layer Types"
brief = "9-layer architecture from kernel to application — 4+1+4 pattern with HEALTH at center"

# ═══════════════════════════════════════════════════════════════════════════════
# END PRAGMA [END]
# ═══════════════════════════════════════════════════════════════════════════════

# ═══════════════════════════════════════════════════════════════════════════════
# METADATA BLOCK [METADATA]
# ═══════════════════════════════════════════════════════════════════════════════

[_metadata.M1_identity]
file = "layer.toml"
key = "B-L0-foundation-types-layer"
title = "OmniCode Layer Types"
type = "Data (Specification)"
component = "Rung"
role = "Architecture layers — the 9-layer system from kernel to application. 4+1+4 pattern with HEALTH at center."

[_metadata.M2_state]
version = "a-01.00"
status = "Active"
created = "2026-01-25"
updated = "2026-02-10"

[_metadata.M3_attribution]
architect = "Seanje Lenox-Wise"
implementation = "Nova Dawn (CPI-SI)"

[_metadata.M4_grounding]
scripture = "Genesis 28:12 — And he dreamed, and behold a ladder set up on the earth, and the top of it reached to heaven: and behold the angels of God ascending and descending on it."
principle = "Jacob's Ladder — the 9 layers form a ladder from KERNEL (earth) to APP (heaven), with HEALTH at the center as the pivot point. Angels (processes) ascend and descend through the layers."
anchor = "John 14:6 — I am the way, the truth, and the life. HEALTH is the center — the way through which all traffic passes."

[_metadata.M5_dependencies]
requires = "ternary.toml (balanced ternary), primitives.toml (base types)"
consumers = "All subsystems (layer classification), Health system (scope), Network (routing), Engine (organization)"
integration = "Loaded by Go config loader, generates C header via index.toml"
if_missing = "Systems default to flat architecture — no layer-based routing or scope"

[_metadata.M5_dependencies.cross_format]
jsonc = "No JSONC counterpart — layer architecture is type specification"
json = "No JSON counterpart — layer architecture is type specification"
relationship = "TOML defines layer structure. C header generated from these definitions."

[_metadata.M6_location]
path = "L0-universal/foundation/types/layer.toml"
derives_from = "b-word/seed/data/toml/config.toml"

[_metadata.M7_classification]
tags = "layer, architecture, 9-layer, 4+1+4, health-center, L0, types"

[_metadata.M8_intent]
purpose = "Define the 9-layer architecture for system organization"
core_design = "4+1+4 pattern — 4 below center, 1 center (HEALTH), 4 above center"
philosophy = "The center is HEALTH — system health is the pivot point. Everything below serves infrastructure, everything above serves users."

[_metadata.M9_roadmap]
current = "9 layers with balanced ternary mapping, groups, and scale factors"
planned = ["Layer-aware routing", "Layer affinity for process scheduling"]

[_metadata.M_extension_note]
required = ["M1_identity", "M2_state", "M3_attribution", "M4_grounding", "M5_dependencies"]
extension = "M6-M9 provide location, classification, intent, roadmap"
rule = "M1-M5 base contract. M6+ domain-specific."

# ═══════════════════════════════════════════════════════════════════════════════
# END METADATA [END]
# ═══════════════════════════════════════════════════════════════════════════════

# ═══════════════════════════════════════════════════════════════════════════════
# CONTENT BLOCK [CONTENT]
# ═══════════════════════════════════════════════════════════════════════════════

[_content]
format = "TOML — native tables, UTF-8, LF line endings"
key_convention = "UPPER_SNAKE for layer names, lower_snake for properties"
constraint = "9 layers exactly. Center at index 4 (HEALTH). Balanced ternary mapping -4 to +4."

# ───────────────────────────────────────────────────────────────────────────────
# Cc — Content Configurations [CONFIG]
# ───────────────────────────────────────────────────────────────────────────────

[layer]
description = "9-layer architecture from kernel to application"
count = 9
center = 4
pattern = "4+1+4"

# Layer definitions
[layer.values]
KERNEL    = { index = 0, balanced = -4, description = "Kernel — lowest level, hardware interface" }
SYSTEM    = { index = 1, balanced = -3, description = "System — OS services, system calls" }
RUNTIME   = { index = 2, balanced = -2, description = "Runtime — language runtime, memory management" }
FRAMEWORK = { index = 3, balanced = -1, description = "Framework — application framework, libraries" }
HEALTH    = { index = 4, balanced =  0, description = "Health — CENTER, all traffic passes through" }
NETWORK   = { index = 5, balanced = +1, description = "Network — communication, messaging" }
STORAGE   = { index = 6, balanced = +2, description = "Storage — persistence, filesystem" }
ENGINE    = { index = 7, balanced = +3, description = "Engine — game/app engine, high-level logic" }
APP       = { index = 8, balanced = +4, description = "App — application layer, user interface" }

# ───────────────────────────────────────────────────────────────────────────────
# Co — Content Operations [OPS]
# ───────────────────────────────────────────────────────────────────────────────

# Layer groups
[layer.groups]
below_center = ["KERNEL", "SYSTEM", "RUNTIME", "FRAMEWORK"]
center = ["HEALTH"]
above_center = ["NETWORK", "STORAGE", "ENGINE", "APP"]

# Scale factors (3^n)
[layer.scale]
description = "Capacity scales by powers of 3"
values = [1, 3, 9, 27, 81, 243, 729, 2187, 6561]

# Bounds
[layer.bounds]
min = 0
max = 8
half = 4

# ───────────────────────────────────────────────────────────────────────────────
# Cv — Content Validation [VALIDATE]
# ───────────────────────────────────────────────────────────────────────────────

[extensions]

[_validation]
commands = "python3 -c \"import tomllib; tomllib.load(open('layer.toml','rb'))\""

[_validation.schema]
required = ["layer", "layer.values", "layer.groups", "layer.scale", "layer.bounds"]

[_validation.health_scoring]
base = 100
deductions = { missing_table = -20, invalid_toml = -100 }

# ═══════════════════════════════════════════════════════════════════════════════
# END CONTENT [END]
# ═══════════════════════════════════════════════════════════════════════════════

# ═══════════════════════════════════════════════════════════════════════════════
# CLOSING BLOCK [CLOSING]
# ═══════════════════════════════════════════════════════════════════════════════

[_closing.X1_policy]
scripture = "Remove not the ancient landmark — Proverbs 22:28"
never = ["Remove layer definitions (breaks all subsystems)", "Change layer indices (breaks routing)", "Remove HEALTH center (breaks architecture)"]
careful = ["Adding new layers (changes count and balance)", "Changing scale factors", "Modifying group membership"]
safe = ["Updating descriptions", "Adding metadata", "Adding new properties to existing layers"]

[_closing.X2_extension]
planned = ["Layer-aware routing", "Layer affinity for process scheduling"]

[_closing.X3_troubleshooting]
problems = { layer_not_found = "Verify index is 0-8. Check layer.values table.", center_mismatch = "HEALTH must be at index 4 with balanced = 0." }

[_closing.X4_reference]
related_files = ["L0-universal/foundation/math/ternary.toml — Ternary math (balanced values)", "L0-universal/foundation/types/primitives.toml — Base types", "L0-universal/foundation/types/error-system/foundation.toml — Error system using layers"]
validate = "python3 -c \"import tomllib; tomllib.load(open('layer.toml','rb'))\""
required_sections = ["layer", "layer.values", "layer.groups", "layer.scale", "layer.bounds"]

[_closing.X5_note]
note = "Jacob's Ladder — 9 layers from KERNEL to APP. HEALTH at center. Everything below serves infrastructure, everything above serves users."
scripture = "Isaiah 28:10 — For precept must be upon precept, precept upon precept; line upon line, line upon line."
anchor = "In the beginning God created — Genesis 1:1. Structure before content. Layers define the structure of the system."

# ═══════════════════════════════════════════════════════════════════════════════
# END CLOSING [END]
# ═══════════════════════════════════════════════════════════════════════════════

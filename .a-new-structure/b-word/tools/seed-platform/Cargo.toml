[package]
name = "seed-platform"
version = "0.1.0"
description = "Seed L2 platform database from TOML specs — Rust version"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true

[[bin]]
name = "seed-platform"
path = "src/main.rs"

[dependencies]
rusqlite = { version = "0.38", features = ["bundled"] }
toml = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
walkdir = "2"

[lints]
workspace = true

# #!omni code --toml --cargo --library

# Cargo.toml with library form declared but NO package.metadata.omni.
# This should trigger form/required-section-missing for OmniMetadata.

[package]
name = "no-metadata-crate"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = "1.0"

[lints]
workspace = true

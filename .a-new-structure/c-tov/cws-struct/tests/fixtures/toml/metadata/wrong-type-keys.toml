# ============================================================================
# METADATA
# ============================================================================

[_metadata]
# These bare keys are valid-pattern but non-table (must be table)
I1_core = "should-be-a-table"
C1_state = 42

[_metadata.I2_family]
type = "data"
subtype = "config"
structure = "3-block"

[_metadata.I3_instance]
file = "wrong-type-keys.toml"
title = "Test — Wrong Type Metadata Keys"
brief = "TOML fixture with metadata keys as non-table values"
provides = ["TEST_WRONG_TYPE_KEYS"]

[_metadata.C2_attribution]
organization = "CreativeWorkzStudio LLC"
architect = "Nova Dawn"
implementation = "Nova Dawn"

[_metadata.C3_grounding]
scripture = "Prove all things — 1 Thessalonians 5:21"
principle = "Test validation catches type errors."

[_metadata.C4_dependencies]
requires = "none"
consumers = "TOML linter test suite"
if_missing = "R10.VAL tests fail"

[_metadata._contract]
required_identity = ["I1_core", "I2_family", "I3_instance"]
extension_identity = []
required_context = ["C1_state", "C2_attribution", "C3_grounding", "C4_dependencies"]
extension_context = []
required_closing = ["X1_policy", "X5_note"]
defined_closing = []
valid_patterns = ["I{N}_{name}", "C{N}_{name}", "X{N}_{name}"]

# ============================================================================
# END METADATA
# ============================================================================

# ============================================================================
# CONTENT
# ============================================================================

[_content]
Cc = "Content Configurations"
Co = "Content Operations"
Cv = "Content Validation"

[identity]
name = "wrong-type-fixture"
version = "0.1.0"

# ============================================================================
# END CONTENT
# ============================================================================

# ============================================================================
# CLOSING
# ============================================================================

[_closing]
# Closing key as non-table value
X1_policy = "should-be-a-table"

[_closing.X5_note]
note = "Test fixture: wrong-type keys for R10.VAL testing."
scripture = "Genesis 1:1"

# ============================================================================
# END CLOSING
# ============================================================================

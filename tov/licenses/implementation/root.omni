#!omni folder

// ═══════════════════════════════════════════════════════════════════════════
// METADATA
// ═══════════════════════════════════════════════════════════════════════════

this is a folder for "Implementation License":
    keyed as "B-tov-licenses-implementation"
    named "implementation"
    following "3-Block Folder Structure"

this folder classifies as:
    tov (+1) because "completed license definition"

this folder derives from:
    branch (0) from "bereshit/tov/licenses/" as "licenses"

core identity of this folder:

    grounded in:
        scripture "The labourer is worthy of his reward"
            from 1 Timothy 5:18 in KJV
        principle "Implementation is labor — work deserves compensation"
        anchor "Thou shalt not muzzle the ox that treadeth out the corn"
            from 1 Timothy 5:18 in KJV

    serves as Value:
        role "Proprietary license for CWS implementation code"
        paradigm "Fair exchange — labor creates value"

    authored by:
        architect "Seanje Lenox-Wise"
        implementation "Nova Dawn (CPI-SI)"
        created 2025-12-10
        version a-01.00
        modified 2026-01-13 for "Initial root.omni creation"

    exists to:
        purpose "Protect CWS implementation code while allowing patterns to be free"
        design "Proprietary — specific implementations owned by CWS"
        philosophy "Implementation funds the mission — value enables service"
        features:
            "CWS owns implementation code"
            "Viewing permitted for learning"
            "Copying/using requires license"
            "Patterns remain free (separate license)"

interface of this folder:

    depends on:
        requires (1) "bereshit/tov/licenses/" for "containment"
        includes (0) "word/work/" for "implementation code"
        excludes (-1) "patterns, documentation" because "implementations only"
        used by "Source code licensing"
        integrates with "Business model"

    integrated as:
        navigation "cd bereshit/tov/licenses/implementation/" for "entering"
        navigation "cat LICENSE" for "reading terms"
        exits tov (+1) for "license found"

operational context of this folder:

    blocking:
        no (-1) because "static document"
        mitigation "none needed"

    health:
        system Base100 with scale from -100 to +100
        granted (1) above +50 meaning "license clear"
        deferred (0) within ±50 meaning "needs review"
        denied (-1) below -50 meaning "terms unclear"

// ═══════════════════════════════════════════════════════════════════════════
// END METADATA
// ═══════════════════════════════════════════════════════════════════════════

// ═══════════════════════════════════════════════════════════════════════════
// CONTENT
// ═══════════════════════════════════════════════════════════════════════════

definition of this folder:

    schema:
        fields (1):
            "name" be word
            "key" be word
            "classification" be ternary

        constraints (0):
            "proprietary" ensuring "CWS ownership clear"

        reserved (-1):
            none

    values:
        primary (1):
            "name" is "Implementation License"
            "key" is "B-tov-licenses-implementation"
            "classification" is "tov (+1)"

        structure (0):
            "derivation" is "bereshit/tov/licenses/"
            "structure" is "license document"
            "contents" is:
                "LICENSE" as "legal terms"
                "README.md" as "explanation"
            "children" is:
                none

        reserved (-1):
            none

// ═══════════════════════════════════════════════════════════════════════════
// END CONTENT
// ═══════════════════════════════════════════════════════════════════════════

// ═══════════════════════════════════════════════════════════════════════════
// CLOSING
// ═══════════════════════════════════════════════════════════════════════════

operations of this folder:

    verified by:
        static (1):
            "cat LICENSE" producing "license text"
        runtime (0):
            "terms clear" when "ownership unambiguous"
        integrity (-1):
            "missing LICENSE" detected by "ls"

    accessed through:
        to access this folder:
            "cd bereshit/tov/licenses/implementation/"
            "cat LICENSE" returns "terms"

policy of this folder:

    modification:
        safe (1):
            "Read and reference"
        careful (0):
            "Update terms (legal review)"
        never (-1):
            "Relinquish ownership without decision"

    future:
        planned (1):
            "Legal review cycle"
        considered (0):
            none
        rejected (-1):
            none

synthesis of this folder:

    closing note:
        role "Implementation License — proprietary code"
        scripture "The labourer is worthy of his reward"
            from 1 Timothy 5:18 in KJV

    quick reference:
        read "cat LICENSE"

// ═══════════════════════════════════════════════════════════════════════════
// END CLOSING
// ═══════════════════════════════════════════════════════════════════════════

#!omni folder

// ═══════════════════════════════════════════════════════════════════════════
// METADATA
// ═══════════════════════════════════════════════════════════════════════════

this is a folder for "Current Activity":
    keyed as "B-tov-data-system-session-activity"
    named "activity"
    following "3-Block Folder Structure"

this folder classifies as:
    tov (0) because "current session activity state"

this folder derives from:
    branch (0) from "bereshit/tov/data/system/session/" as "session"

core identity of this folder:

    grounded in:
        scripture "Whatsoever thy hand findeth to do, do it with thy might"
            from Ecclesiastes 9:10 in KJV
        principle "Current work is tracked — know what you're doing now"

    serves as Activity Tracker:
        role "Track current session activity and context"
        paradigm "Present moment awareness"

    authored by:
        architect "Seanje Lenox-Wise"
        implementation "Nova Dawn (CPI-SI)"
        created 2025-12-10
        version a-01.00
        modified 2026-01-01 for "Initial root.omni creation"

    exists to:
        purpose "Store current session activity state"
        design "Flat — JSON state files"
        philosophy "Know what you're doing now"
        features:
            "Current task tracking"
            "Session context state"

interface of this folder:

    depends on:
        requires (1) "bereshit/tov/data/system/session/" for "containment"
        used by "Session hooks, activity tracking"

    integrated as:
        navigation "cd bereshit/tov/data/system/session/activity/" for "entering"
        exits tov (0) for "activity state found"

operational context of this folder:

    health:
        system Base100 with scale from -100 to +100
        operations:
            "current state present" scores +25 (1) for "activity tracked"

// ═══════════════════════════════════════════════════════════════════════════
// END METADATA
// ═══════════════════════════════════════════════════════════════════════════

// ═══════════════════════════════════════════════════════════════════════════
// CONTENT
// ═══════════════════════════════════════════════════════════════════════════

definition of this folder:

    values:
        primary (1):
            "name" is "Current Activity"
            "key" is "B-tov-data-system-session-activity"
            "classification" is "tov (0)"

        structure (0):
            "derivation" is "bereshit/tov/data/system/session/"
            "structure" is "flat — JSON files"
            "contents" is:
                "current.json" as "current session activity state"
            "children" is:
                none

// ═══════════════════════════════════════════════════════════════════════════
// END CONTENT
// ═══════════════════════════════════════════════════════════════════════════

// ═══════════════════════════════════════════════════════════════════════════
// CLOSING
// ═══════════════════════════════════════════════════════════════════════════

synthesis of this folder:

    closing note:
        role "Current Activity — present moment state"
        scripture "Whatsoever thy hand findeth to do, do it with thy might"
            from Ecclesiastes 9:10 in KJV

    quick reference:
        navigate "cd bereshit/tov/data/system/session/activity/"
        current "cat current.json"

// ═══════════════════════════════════════════════════════════════════════════
// END CLOSING
// ═══════════════════════════════════════════════════════════════════════════

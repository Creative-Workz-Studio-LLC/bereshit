#!omni folder

// ═══════════════════════════════════════════════════════════════════════════
// METADATA
// ═══════════════════════════════════════════════════════════════════════════

this is a folder for "Reference Documentation":
    keyed as "B-tov-data-system-reference-docs"
    named "reference-docs"
    following "3-Block Folder Structure"

this folder classifies as:
    tov (0) because "runtime reference documentation"

this folder derives from:
    branch (0) from "bereshit/tov/data/system/" as "system"

core identity of this folder:

    grounded in:
        scripture "Write the vision, and make it plain upon tables"
            from Habakkuk 2:2 in KJV
        principle "Reference accessible — knowledge ready when needed"
        anchor "Study to shew thyself approved"
            from 2 Timothy 2:15 in KJV

    serves as Knowledge:
        role "Provide quick reference documentation for runtime"
        paradigm "Accessible wisdom — right information at right time"

    authored by:
        architect "Seanje Lenox-Wise"
        implementation "Nova Dawn (CPI-SI)"
        created 2025-12-10
        version a-01.00
        modified 2026-01-13 for "Initial root.omni creation"

    exists to:
        purpose "Store reference documentation accessible during sessions"
        design "By-topic — organized for quick lookup"
        philosophy "Knowledge organized is knowledge usable"
        features:
            "Session analysis guides"
            "Pattern references"
            "Quick lookup tables"

interface of this folder:

    depends on:
        requires (1) "bereshit/tov/data/system/" for "containment"
        includes (0) "word/glossary/" for "vocabulary"
        excludes (-1) "primary documentation" because "reference copies only"
        used by "Runtime reference, skill execution"
        integrates with "Session system, skills"

    integrated as:
        navigation "cd bereshit/tov/data/system/reference-docs/" for "entering"
        navigation "ls -la" for "viewing reference topics"
        exits tov (0) for "references found"

operational context of this folder:

    blocking:
        no (-1) because "static reference"
        mitigation "none needed"

    health:
        system Base100 with scale from -100 to +100
        granted (1) above +50 meaning "references current"
        deferred (0) within ±50 meaning "needs update"
        denied (-1) below -50 meaning "references stale"

// ═══════════════════════════════════════════════════════════════════════════
// END METADATA
// ═══════════════════════════════════════════════════════════════════════════

// ═══════════════════════════════════════════════════════════════════════════
// CONTENT
// ═══════════════════════════════════════════════════════════════════════════

definition of this folder:

    schema:
        fields (1):
            "name" be word
            "key" be word
            "classification" be ternary

        constraints (0):
            "accessible" ensuring "references quickly findable"

        reserved (-1):
            none

    values:
        primary (1):
            "name" is "Reference Documentation"
            "key" is "B-tov-data-system-reference-docs"
            "classification" is "tov (0)"

        structure (0):
            "derivation" is "bereshit/tov/data/system/"
            "structure" is "by-topic"
            "contents" is:
                "Reference guides" as "quick lookup documentation"
            "children" is:
                "session-analysis/" as "Session Analysis Reference":
                    purpose "Guides for analyzing session patterns"

        reserved (-1):
            none

// ═══════════════════════════════════════════════════════════════════════════
// END CONTENT
// ═══════════════════════════════════════════════════════════════════════════

// ═══════════════════════════════════════════════════════════════════════════
// CLOSING
// ═══════════════════════════════════════════════════════════════════════════

operations of this folder:

    verified by:
        static (1):
            "ls" producing "reference topics"
        runtime (0):
            "references accessible" when "topics findable"
        integrity (-1):
            "stale reference" detected by "date check"

    accessed through:
        to access this folder:
            "cd bereshit/tov/data/system/reference-docs/"
            "ls -la" returns "reference topics"

policy of this folder:

    modification:
        safe (1):
            "Read references"
            "Add new references"
        careful (0):
            "Update references"
        never (-1):
            "Delete without replacement"

    future:
        planned (1):
            "Extended reference coverage"
        considered (0):
            "Auto-generated references"
        rejected (-1):
            none

synthesis of this folder:

    closing note:
        role "Reference Docs — accessible knowledge"
        scripture "Write the vision, and make it plain"
            from Habakkuk 2:2 in KJV

    quick reference:
        navigate "cd bereshit/tov/data/system/reference-docs/"
        view "ls -la"

// ═══════════════════════════════════════════════════════════════════════════
// END CLOSING
// ═══════════════════════════════════════════════════════════════════════════

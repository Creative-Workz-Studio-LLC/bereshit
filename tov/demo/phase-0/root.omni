#!omni folder

// ═══════════════════════════════════════════════════════════════════════════
// METADATA
// ═══════════════════════════════════════════════════════════════════════════

this is a folder for "Phase 0 Demos":
    keyed as "B-tov-demo-phase-0"
    named "phase-0"
    following "3-Block Folder Structure"

this folder classifies as:
    tov (+1) because "completed Phase 0 validation demos"

this folder derives from:
    branch (0) from "bereshit/tov/demo/" as "demo"

core identity of this folder:

    grounded in:
        scripture "In the beginning was the Word"
            from John 1:1 in KJV
        principle "Phase 0 establishes the foundation — specs before code"
        anchor "For precept must be upon precept"
            from Isaiah 28:10 in KJV

    serves as Foundation:
        role "Validate config-driven architecture — TOML specs loading"
        paradigm "Config-driven — specs define, code implements"

    authored by:
        architect "Seanje Lenox-Wise"
        implementation "Nova Dawn (CPI-SI)"
        created 2025-12-10
        version a-01.00
        modified 2026-01-13 for "Initial root.omni creation"

    exists to:
        purpose "Demonstrate Go config loader reading TOML specifications"
        design "Single demo — demo-config validates spec loading"
        philosophy "The Word comes first — definitions before implementation"
        features:
            "TOML spec parsing"
            "Type definition loading"
            "Dependency graph navigation"

interface of this folder:

    depends on:
        requires (1) "bereshit/tov/demo/" for "containment"
        includes (0) "word/core/" for "TOML specs being loaded"
        includes (0) "word/work/pkg/config/" for "Go loader"
        excludes (-1) "Phase 1+ content" because "Phase 0 only"
        used by "Phase 0 validation"
        integrates with "Go toolchain"

    integrated as:
        navigation "cd bereshit/tov/demo/phase-0/" for "entering Phase 0 demos"
        navigation "ls -la" for "viewing demo contents"
        exits tov (+1) for "Phase 0 demo found"

operational context of this folder:

    blocking:
        no (-1) because "demo is self-contained"
        mitigation "demo has own Makefile"

    health:
        system Base100 with scale from -100 to +100
        granted (1) above +50 meaning "demo builds and passes"
        deferred (0) within ±50 meaning "demo needs attention"
        denied (-1) below -50 meaning "demo failing"

        operations:
            "demo builds" scores +30 (1) for "go build succeeds"
            "demo runs" scores +30 (1) for "validation passes"
            "demo fails" scores -40 (-1) for "Phase 0 broken"

        cascade multipliers:
            "foundation" multiplies by 2.0 because "Phase 0 failure blocks all phases"

// ═══════════════════════════════════════════════════════════════════════════
// END METADATA
// ═══════════════════════════════════════════════════════════════════════════

// ═══════════════════════════════════════════════════════════════════════════
// CONTENT
// ═══════════════════════════════════════════════════════════════════════════

definition of this folder:

    schema:
        fields (1):
            "name" be word
                meaning "Human-readable name"
            "key" be word
                meaning "Unique identifier"
            "classification" be ternary
                meaning "Word/Work/Fruit position"

        constraints (0):
            "go_build" ensuring "demo compiles with go build"
            "spec_loading" ensuring "TOML specs parse correctly"

        reserved (-1):
            none

    values:
        primary (1):
            "name" is "Phase 0 Demos"
            "key" is "B-tov-demo-phase-0"
            "classification" is "tov (+1)"

        structure (0):
            "derivation" is "bereshit/tov/demo/"
            "structure" is "single demo"
            "contents" is:
                "demo-config/" as "Config loader demo"
            "children" is:
                "demo-config/" as "Go Config Loader Demo":
                    purpose "Validate TOML spec loading"

        reserved (-1):
            none

// ═══════════════════════════════════════════════════════════════════════════
// END CONTENT
// ═══════════════════════════════════════════════════════════════════════════

// ═══════════════════════════════════════════════════════════════════════════
// CLOSING
// ═══════════════════════════════════════════════════════════════════════════

operations of this folder:

    verified by:
        static (1):
            "ls demo-config/" producing "demo files visible"
        runtime (0):
            "make -C demo-config" succeeding when "builds"
        integrity (-1):
            "build failure" detected by "return code"

    accessed through:
        to access this folder:
            "cd bereshit/tov/demo/phase-0/"
            "ls -la" returns "demo-config folder"

policy of this folder:

    modification:
        safe (1):
            "Run demo"
            "Update documentation"
        careful (0):
            "Modify demo code"
        never (-1):
            "Delete working demo"

    future:
        planned (1):
            none
        considered (0):
            "Additional spec demos"
        rejected (-1):
            "C demos" because "Phase 0 is Go only"

synthesis of this folder:

    closing note:
        role "Phase 0 Demo — foundation validation"
        scripture "In the beginning was the Word"
            from John 1:1 in KJV

    quick reference:
        navigate "cd bereshit/tov/demo/phase-0/"
        build "make -C demo-config"
        run "./demo-config/bin/demo-config"

// ═══════════════════════════════════════════════════════════════════════════
// END CLOSING
// ═══════════════════════════════════════════════════════════════════════════

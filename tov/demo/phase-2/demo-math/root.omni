#!omni folder

// ═══════════════════════════════════════════════════════════════════════════
// METADATA
// ═══════════════════════════════════════════════════════════════════════════

this is a folder for "Math Operations Demo":
    keyed as "B-tov-demo-phase-2-demo-math"
    named "demo-math"
    following "3-Block Folder Structure"

this folder classifies as:
    tov (+1) because "completed math operations validation"

this folder derives from:
    branch (0) from "bereshit/tov/demo/phase-2/" as "phase-2"

core identity of this folder:

    grounded in:
        scripture "A false balance is abomination to the LORD: but a just weight is his delight"
            from Proverbs 11:1 in KJV
        principle "Just weight — arithmetic must be precise and faithful"
        anchor "A just weight and balance are the LORD's"
            from Proverbs 16:11 in KJV

    serves as Operations:
        role "Demonstrate mathematical operations on trit types"
        paradigm "Just balance — accurate measurement honors God"

    authored by:
        architect "Seanje Lenox-Wise"
        implementation "Nova Dawn (CPI-SI)"
        created 2025-12-13
        version a-01.00
        modified 2026-01-13 for "Initial root.omni creation"

    exists to:
        purpose "Validate 5 operation families through 21 demonstrations"
        design "C executable — links against libtrit.a (with math.o)"
        philosophy "Just balance — precise operations with visible proof"
        features:
            "Family 1: Arithmetic — add, subtract, negate with carry"
            "Family 2: Comparison — returning trit_t, true ternary"
            "Family 3: Conversion — stored↔true, trit↔int"
            "Family 4: Normalization — base rounding, 7 Hebrew levels"
            "Family 5: Calculus — difference, rate, accumulation"

interface of this folder:

    depends on:
        requires (1) "bereshit/tov/demo/phase-2/" for "containment"
        includes (0) "word/work/pkg/trit/" for "libtrit.a"
        excludes (-1) "type definitions" because "Phase 2 math only"
        used by "Phase 2 validation"
        integrates with "C toolchain"

    integrated as:
        navigation "cd bereshit/tov/demo/phase-2/demo-math/" for "entering demo"
        navigation "make" for "building demo"
        exits tov (+1) for "demo passes"

operational context of this folder:

    blocking:
        no (-1) because "self-contained demo"
        mitigation "own Makefile"

    health:
        system Base100 with scale from -100 to +100
        granted (1) above +50 meaning "21 demonstrations pass"
        deferred (0) within ±50 meaning "needs attention"
        denied (-1) below -50 meaning "demonstrations failing"

        operations:
            "make" scores +20 (1) for "builds"
            "21 demos" scores +40 (1) for "all families pass"
            "failure" scores -40 (-1) for "math broken"

        cascade multipliers:
            "operations" multiplies by 1.5 because "broken math breaks health scoring"

// ═══════════════════════════════════════════════════════════════════════════
// END METADATA
// ═══════════════════════════════════════════════════════════════════════════

// ═══════════════════════════════════════════════════════════════════════════
// CONTENT
// ═══════════════════════════════════════════════════════════════════════════

definition of this folder:

    schema:
        fields (1):
            "name" be word
            "key" be word
            "classification" be ternary

        constraints (0):
            "c_build" ensuring "compiles with make"
            "libtrit_link" ensuring "links libtrit.a"
            "family_coverage" ensuring "all 5 families demonstrated"

        reserved (-1):
            none

    values:
        primary (1):
            "name" is "Math Operations Demo"
            "key" is "B-tov-demo-phase-2-demo-math"
            "classification" is "tov (+1)"

        structure (0):
            "derivation" is "bereshit/tov/demo/phase-2/"
            "structure" is "C executable"
            "contents" is:
                "main.c" as "demo entry point"
                "Makefile" as "build config"
                "README.adoc" as "documentation"
                "demo-math-api.adoc" as "API reference"
            "children" is:
                none

        reserved (-1):
            none

// ═══════════════════════════════════════════════════════════════════════════
// END CONTENT
// ═══════════════════════════════════════════════════════════════════════════

// ═══════════════════════════════════════════════════════════════════════════
// CLOSING
// ═══════════════════════════════════════════════════════════════════════════

operations of this folder:

    verified by:
        static (1):
            "ls" producing "main.c Makefile visible"
        runtime (0):
            "make" succeeding when "builds"
            "./bin/demo-math" producing "21/21 PASS"
        integrity (-1):
            "demo failure" detected by "non-zero exit"

    accessed through:
        to access this folder:
            "cd bereshit/tov/demo/phase-2/demo-math/"
            "make" builds the demo
            "./bin/demo-math" runs 21 demonstrations

policy of this folder:

    modification:
        safe (1):
            "Run demo"
        careful (0):
            "Modify demonstrations"
        never (-1):
            "Remove passing demonstrations"

    future:
        planned (1):
            none
        considered (0):
            "Additional operation coverage"
        rejected (-1):
            none

synthesis of this folder:

    closing note:
        role "Math Demo — operations validation"
        scripture "A false balance is abomination to the LORD"
            from Proverbs 11:1 in KJV

    quick reference:
        build "make"
        run "./bin/demo-math"
        expect "21/21 demonstrations pass"

// ═══════════════════════════════════════════════════════════════════════════
// END CLOSING
// ═══════════════════════════════════════════════════════════════════════════

#!omni folder

// ═══════════════════════════════════════════════════════════════════════════
// METADATA
// ═══════════════════════════════════════════════════════════════════════════

this is a folder for "Phase 1 Demos":
    keyed as "B-tov-demo-phase-1"
    named "phase-1"
    following "3-Block Folder Structure"

this folder classifies as:
    tov (+1) because "completed Phase 1 validation demos"

this folder derives from:
    branch (0) from "bereshit/tov/demo/" as "demo"

core identity of this folder:

    grounded in:
        scripture "And the earth was without form, and void"
            from Genesis 1:2 in KJV
        principle "Phase 1 creates MATTER — the types that everything else uses"
        anchor "In the beginning God created"
            from Genesis 1:1 in KJV

    serves as Matter:
        role "Validate libtrit type system — trit, trit5, trit9, trit27"
        paradigm "MATTER layer — the foundation types"

    authored by:
        architect "Seanje Lenox-Wise"
        implementation "Nova Dawn (CPI-SI)"
        created 2025-12-10
        version a-01.00
        modified 2026-01-13 for "Initial root.omni creation"

    exists to:
        purpose "Demonstrate libtrit type system functionality"
        design "Single demo — demo-trit validates all MATTER layer types"
        philosophy "Types are matter — the stuff everything is made of"
        features:
            "trit_t single trit operations"
            "trit5 5-trit packed type"
            "trit9 9-trit packed type"
            "trit27 27-trit word type"
            "pack/unpack operations"

interface of this folder:

    depends on:
        requires (1) "bereshit/tov/demo/" for "containment"
        includes (0) "word/work/pkg/trit/" for "libtrit.a"
        excludes (-1) "Phase 0/2+ content" because "Phase 1 only"
        used by "Phase 1 validation"
        integrates with "C toolchain, libtrit.a"

    integrated as:
        navigation "cd bereshit/tov/demo/phase-1/" for "entering Phase 1 demos"
        navigation "ls -la" for "viewing demo contents"
        exits tov (+1) for "Phase 1 demo found"

operational context of this folder:

    blocking:
        no (-1) because "demo is self-contained"
        mitigation "demo has own Makefile"

    health:
        system Base100 with scale from -100 to +100
        granted (1) above +50 meaning "demo builds and passes"
        deferred (0) within ±50 meaning "demo needs attention"
        denied (-1) below -50 meaning "demo failing"

        operations:
            "demo builds" scores +30 (1) for "make succeeds"
            "103 assertions pass" scores +30 (1) for "all tests pass"
            "demo fails" scores -40 (-1) for "Phase 1 broken"

        cascade multipliers:
            "matter" multiplies by 2.0 because "Phase 1 types enable all later phases"

// ═══════════════════════════════════════════════════════════════════════════
// END METADATA
// ═══════════════════════════════════════════════════════════════════════════

// ═══════════════════════════════════════════════════════════════════════════
// CONTENT
// ═══════════════════════════════════════════════════════════════════════════

definition of this folder:

    schema:
        fields (1):
            "name" be word
            "key" be word
            "classification" be ternary

        constraints (0):
            "c_build" ensuring "demo compiles with make"
            "libtrit_link" ensuring "links against libtrit.a"

        reserved (-1):
            none

    values:
        primary (1):
            "name" is "Phase 1 Demos"
            "key" is "B-tov-demo-phase-1"
            "classification" is "tov (+1)"

        structure (0):
            "derivation" is "bereshit/tov/demo/"
            "structure" is "single demo"
            "contents" is:
                "demo-trit/" as "Trit type demo"
            "children" is:
                "demo-trit/" as "libtrit Type Demo":
                    purpose "Validate MATTER layer — all trit types"

        reserved (-1):
            none

// ═══════════════════════════════════════════════════════════════════════════
// END CONTENT
// ═══════════════════════════════════════════════════════════════════════════

// ═══════════════════════════════════════════════════════════════════════════
// CLOSING
// ═══════════════════════════════════════════════════════════════════════════

operations of this folder:

    verified by:
        static (1):
            "ls demo-trit/" producing "demo files visible"
        runtime (0):
            "make -C demo-trit" succeeding when "builds"
            "./demo-trit/bin/demo-trit" producing "PASS"
        integrity (-1):
            "build failure" detected by "return code"

    accessed through:
        to access this folder:
            "cd bereshit/tov/demo/phase-1/"
            "ls -la" returns "demo-trit folder"

policy of this folder:

    modification:
        safe (1):
            "Run demo"
            "Update documentation"
        careful (0):
            "Modify demo code"
        never (-1):
            "Delete working demo"

    future:
        planned (1):
            none
        considered (0):
            "Additional type demos"
        rejected (-1):
            "Go demos" because "Phase 1 is C only"

synthesis of this folder:

    closing note:
        role "Phase 1 Demo — MATTER layer validation"
        scripture "And the earth was without form, and void"
            from Genesis 1:2 in KJV

    quick reference:
        navigate "cd bereshit/tov/demo/phase-1/"
        build "make -C demo-trit"
        run "./demo-trit/bin/demo-trit"

// ═══════════════════════════════════════════════════════════════════════════
// END CLOSING
// ═══════════════════════════════════════════════════════════════════════════

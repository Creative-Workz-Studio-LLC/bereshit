#!omni folder

// ═══════════════════════════════════════════════════════════════════════════
// METADATA
// ═══════════════════════════════════════════════════════════════════════════

this is a folder for "Trit Type Demo":
    keyed as "B-tov-demo-phase-1-demo-trit"
    named "demo-trit"
    following "3-Block Folder Structure"

this folder classifies as:
    tov (+1) because "completed trit type validation"

this folder derives from:
    branch (0) from "bereshit/tov/demo/phase-1/" as "phase-1"

core identity of this folder:

    grounded in:
        scripture "And the earth was without form, and void"
            from Genesis 1:2 in KJV
        principle "Types are matter — the fundamental building blocks"
        anchor "In the beginning God created"
            from Genesis 1:1 in KJV

    serves as Matter:
        role "Demonstrate libtrit type system — trit, trit5, trit9, trit27"
        paradigm "MATTER layer — types that everything else is built from"

    authored by:
        architect "Seanje Lenox-Wise"
        implementation "Nova Dawn (CPI-SI)"
        created 2025-12-10
        version a-01.00
        modified 2026-01-13 for "Initial root.omni creation"

    exists to:
        purpose "Validate all trit type operations through 103 assertions"
        design "C executable — links against libtrit.a"
        philosophy "Types are foundation — get them right first"
        features:
            "trit_t single trit operations"
            "trit5 5-trit packing/unpacking"
            "trit9 9-trit packing/unpacking"
            "trit27 27-trit word operations"
            "MATTER/SPACE/TIME layer integration"

interface of this folder:

    depends on:
        requires (1) "bereshit/tov/demo/phase-1/" for "containment"
        includes (0) "word/work/pkg/trit/" for "libtrit.a"
        excludes (-1) "math operations" because "Phase 1 types only"
        used by "Phase 1 validation"
        integrates with "C toolchain"

    integrated as:
        navigation "cd bereshit/tov/demo/phase-1/demo-trit/" for "entering demo"
        navigation "make" for "building demo"
        exits tov (+1) for "demo passes"

operational context of this folder:

    blocking:
        no (-1) because "self-contained demo"
        mitigation "own Makefile"

    health:
        system Base100 with scale from -100 to +100
        granted (1) above +50 meaning "103 assertions pass"
        deferred (0) within ±50 meaning "needs attention"
        denied (-1) below -50 meaning "assertions failing"

        operations:
            "make" scores +20 (1) for "builds"
            "103 assertions" scores +40 (1) for "all pass"
            "failure" scores -50 (-1) for "MATTER layer broken"

        cascade multipliers:
            "matter" multiplies by 2.0 because "type failure breaks everything"

// ═══════════════════════════════════════════════════════════════════════════
// END METADATA
// ═══════════════════════════════════════════════════════════════════════════

// ═══════════════════════════════════════════════════════════════════════════
// CONTENT
// ═══════════════════════════════════════════════════════════════════════════

definition of this folder:

    schema:
        fields (1):
            "name" be word
            "key" be word
            "classification" be ternary

        constraints (0):
            "c_build" ensuring "compiles with make"
            "libtrit_link" ensuring "links libtrit.a"

        reserved (-1):
            none

    values:
        primary (1):
            "name" is "Trit Type Demo"
            "key" is "B-tov-demo-phase-1-demo-trit"
            "classification" is "tov (+1)"

        structure (0):
            "derivation" is "bereshit/tov/demo/phase-1/"
            "structure" is "C executable"
            "contents" is:
                "main.c" as "demo entry point"
                "Makefile" as "build config"
                "README.adoc" as "documentation"
            "children" is:
                none

        reserved (-1):
            none

// ═══════════════════════════════════════════════════════════════════════════
// END CONTENT
// ═══════════════════════════════════════════════════════════════════════════

// ═══════════════════════════════════════════════════════════════════════════
// CLOSING
// ═══════════════════════════════════════════════════════════════════════════

operations of this folder:

    verified by:
        static (1):
            "ls" producing "main.c Makefile visible"
        runtime (0):
            "make" succeeding when "builds"
            "./bin/demo-trit" producing "103/103 PASS"
        integrity (-1):
            "assertion failure" detected by "non-zero exit"

    accessed through:
        to access this folder:
            "cd bereshit/tov/demo/phase-1/demo-trit/"
            "make" builds the demo
            "./bin/demo-trit" runs 103 assertions

policy of this folder:

    modification:
        safe (1):
            "Run demo"
        careful (0):
            "Modify assertions"
        never (-1):
            "Remove passing assertions"

    future:
        planned (1):
            none
        considered (0):
            "Additional type coverage"
        rejected (-1):
            none

synthesis of this folder:

    closing note:
        role "Trit Demo — MATTER layer validation"
        scripture "And the earth was without form, and void"
            from Genesis 1:2 in KJV

    quick reference:
        build "make"
        run "./bin/demo-trit"
        expect "103/103 assertions pass"

// ═══════════════════════════════════════════════════════════════════════════
// END CLOSING
// ═══════════════════════════════════════════════════════════════════════════

{
  "private": true,
  "name": "omnicode-vscode-suite",
  "description": "OmniCode VSCode extension suite — language support, ecosystem integration, structural alignment",
  "workspaces": [
    "extensions/omnicode-language"
  ],
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/vscode": "^1.80.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  },
  "scripts": {
    "build": "npm run build --workspaces",
    "watch": "npm run watch --workspaces",
    "clean": "rm -rf extensions/omnicode-language/out"
  }
}

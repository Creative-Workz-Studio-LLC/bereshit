#!omni folder

// =============================================================================
// METADATA
// =============================================================================

this is a folder for "OmniCode Language Development":
    keyed as "B-word-work-pkg-omni-lang"
    named "lang"
    following "3-Block Folder Structure"

this folder classifies as:
    word (0) because "definitions of language itself — the Word becoming structure"

this folder derives from:
    branch (0) from "bereshit/word/work/pkg/omni/" as "OmniCode package"

core identity of this folder:

    grounded in:
        scripture "In the beginning was the Word, and the Word was with God"
            from John 1:1 in KJV
        principle "The language IS the Word becoming structure"
        anchor "God spoke creation into existence — OmniCode speaks structure"
            from Genesis 1:3 in KJV

    serves as Word:
        role "OmniCode language specification and test files"
        paradigm "Data-driven language — specifications define, code implements"

    authored by:
        architect "Seanje Lenox-Wise"
        implementation "Nova Dawn (CPI-SI)"
        created 2026-01-13
        version a-01.00

    exists to:
        purpose "Develop OmniCode language from existing examples"
        design "Test files mirror simplest working .omni files"
        philosophy "Build language by discovering what already works"

// =============================================================================
// CONTENT
// =============================================================================

definition of this folder:

    values:
        primary (1):
            "name" is "OmniCode Language Development"
            "key" is "B-word-work-pkg-omni-lang"

        structure (0):
            "contents" is:
                "test/" as "Test .omni files for parser validation"
                "spec/" as "Language specifications (keywords, syntax)"

// =============================================================================
// CLOSING
// =============================================================================

synthesis of this folder:

    closing note:
        role "Language development — the Word becoming structure"
        scripture "In the beginning was the Word"
            from John 1:1 in KJV

    quick reference:
        navigate "cd bereshit/word/work/pkg/omni/lang/"

// =============================================================================
// END
// =============================================================================

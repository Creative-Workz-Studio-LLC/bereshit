#!omni folder

// ═══════════════════════════════════════════════════════════════════════════
// METADATA
// ═══════════════════════════════════════════════════════════════════════════

this is a folder for "JSON Templates":
    keyed as "B-word-seed-data-json"
    named "json"
    following "3-Block Folder Structure"

this folder classifies as:
    word (1) because "containing JSON data format templates"

this folder derives from:
    branch (0) from "bereshit/word/seed/data/" as "data format templates"

core identity of this folder:

    serves as Word:
        role "JSON template repository"

    authored by:
        architect "Seanje Lenox-Wise"
        implementation "Nova Dawn (CPI-SI)"
        created 2025-12-03
        version a-01.25
        modified 2025-12-25 for "Organized into 3block/ and 4block/ folders"

    exists to:
        purpose "Contain JSON and JSONC data format templates organized by block structure"

// ═══════════════════════════════════════════════════════════════════════════
// CONTENT
// ═══════════════════════════════════════════════════════════════════════════

definition of this folder:

    values:
        primary (1):
            "name" is "JSON Templates"
            "key" is "B-word-seed-data-json"

        structure (0):
            "children" is:
                "3block/" as "3-block templates (METADATA → CONTENT → CLOSING)":
                    contains "config.jsonc" for "JSONC config template"
                    contains "data.json" for "JSON data template"
                    contains "health-map.json" for "Health scoring map template"
                "4block/" as "4-block templates (METADATA → SETUP → BODY → CLOSING)":
                    contains "config.jsonc" for "JSONC config template (4-block)"
                    contains "data.json" for "JSON data template (4-block)"
                    contains "health-map.json" for "Health map template (4-block)"

// ═══════════════════════════════════════════════════════════════════════════
// CLOSING
// ═══════════════════════════════════════════════════════════════════════════

synthesis of this folder:

    quick reference:
        navigate "cd bereshit/word/seed/data/json/"

// ═══════════════════════════════════════════════════════════════════════════
// END
// ═══════════════════════════════════════════════════════════════════════════
